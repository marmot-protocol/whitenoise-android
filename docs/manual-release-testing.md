# White Noise Android manual release testing

This is the release-candidate checklist for every user-visible White Noise Android surface. It is written so a tester who has never used White Noise can follow it without reading the source. Run every applicable point against the exact APK proposed for release. Do not infer a pass from an automated test.

## How to use this guide

1. Record the APK filename, SHA-256, version, build variant, commit SHA, Android version, device model, tester, and date in the run report.
2. Work from a clean install unless a point explicitly requires retained data or an upgrade. Keep one separate populated installation for upgrade and history checks.
3. Tick a point only after performing its **Actions** and observing its **Expected** result. Record `N/A` only when the point's stated prerequisite is unavailable, and write why.
4. Report failures by the stable point ID, for example `MSG-006`, plus account, conversation type, exact actions, actual result, screenshot or screen recording when safe, and relevant time. IDs are permanent: they are never renumbered or reused.
5. When a point says “the other device,” use another real installation signed in to a different test identity. Allow normal relay latency before failing a synchronization point.
6. “Back” means test both the visible back arrow and Android system Back where both are available. A screen passes only if neither path exits unexpectedly, loses a draft, or returns to the wrong account.

## Safety and test-data rules

- Use disposable test identities, test relay data, synthetic names, synthetic phone contacts, harmless files, and a non-sensitive test location. Never paste a real `nsec`, recovery phrase, contact book, private photo, audit log, or precise home/work location into a report.
- Treat account keys, encrypted backups, QR codes, notification content, exported audit logs, and the Android share sheet as sensitive. Blur or omit their values from evidence.
- Use a dedicated temporary directory for downloads and exports. Delete it, clear the clipboard, remove test accounts, and revoke temporary permissions after the run.
- Before destructive points (wipe, leave, remove member, delete for everyone, disappearing messages), confirm that every affected identity and conversation is disposable.
- Test notification text on a locked device only with synthetic messages. Do not expose private content on a shared lock screen.
- Never approve an unexpected Android permission prompt. A requested permission must correspond to the action described in the current point.
- Use only tester-controlled identities and test relays. Never invite, message, or add an account that the tester does not control. Relay-published profile, membership, and message data can be copied permanently even after local deletion, so use only synthetic, non-sensitive content. Prefer a dedicated test/staging relay set; if none is available, clearly mark the run as using public relays and keep all content disposable.

## Test matrix and prerequisites

- One supported Android phone with a secure device credential; a second Android device or emulator; and, when available, one Android 13+ device for notification permission behavior.
- Three disposable identities: Alice (primary tester), Bob (peer), and Carol (group member). Keep Alice signed in on the release candidate and Bob/Carol on independent installations.
- One direct chat with Bob; one three-person group with Alice as admin; one group where Alice is a non-admin; an invite awaiting Alice; populated and empty/archived chats; and messages from each member.
- Safe fixtures: short and long text, emoji, a web URL, `@`-mentionable members, small/large images, video, audio, PDF, plain-text document, unsupported file, synthetic vCard contact, and a coarse test location.
- Test both the Zapstore and Play build behavior when producing those artifacts. Build-specific points may be `N/A` for a release that does not ship that variant.
- For accessibility coverage, enable TalkBack, large display/font settings, an RTL locale, landscape, dark theme, and reduced-animation settings one at a time.

## Smoke pass

Before the exhaustive pass, run these release-blocking paths in order: `INT-001`, `ONB-003`, `CHL-002`, `CON-005`, `MED-003`, `MSG-005`, `GRP-012`, `NTF-009`, `SEC-002`, `SET-025`, and `END-004`. A smoke failure stops the release but does not replace the full checklist after repair.

## Full release checklist

| Prefix | Area |
|---|---|
| `AXY` | Accessibility and layout |
| `ACC` | Accounts and account keys |
| `CHL` | Chat list and chat actions |
| `CON` | Conversation and composer |
| `DIC` | Dictation |
| `END` | Completion and verdict |
| `FIND` | Search |
| `FLD` | Chat folders |
| `GRP` | Groups and invites |
| `INT` | Install, startup, updates, and platform integration |
| `MED` | Attachments and media |
| `MSG` | Message rendering and actions |
| `NAV` | Navigation and account switching |
| `NCH` | Starting chats and groups |
| `NTF` | Notifications and background delivery |
| `ONB` | Onboarding and sign-in |
| `PRO` | Profiles |
| `RDR` | Nostr readers |
| `SEC` | Device privacy and app lock |
| `SET` | Settings and customization |
| `SYS` | Relays and system intents |
| `TTS` | Text to speech |

### Release identity and first launch

1. [ ] **INT-001 — APK identity** — Install the recorded APK and open Android App info. → **Expected:** The app name, icon, package, version, and build variant match the release candidate; a development or staging build is visibly distinguishable from production.
2. [ ] **INT-002 — Clean launch** — Clear app storage, force-stop, then launch from the launcher. → **Expected:** The system splash hands off to White Noise once, startup never shows another account's content, and onboarding appears without a crash or blank frame. While native bootstrap runs, the startup screen shows the White Noise mark, a small indicator and "Starting White Noise…" together in the middle of the screen in light, dark and AMOLED themes, not a lone indicator at the top.
3. [ ] **INT-003 — Startup progress** — Launch on a slow connection in light and dark themes. → **Expected:** The native splash mark remains legible against its background and promptly hands off to a meaningful, responsive secure-startup/loading state that resolves to onboarding or the signed-in surface. With a previously saved profile, force-stop/relaunch and install a compatible update in place while offline: the first Chats frame uses the active account’s saved name and avatar reference. Record avatar metadata readiness separately from whether image pixels were already cached; a true local profile miss may show initials. Repeat after editing/removing the picture and during rapid account switches; an old account’s profile must never appear. If runtime recovery interrupts activation, check that the recovered account's profile and notification preferences remain correct; deferred refresh steps from the replaced runtime must not start afterward. The startup surface uses indeterminate native loading feedback and remains distinct from an interactive first-useful frame; retained usable chat content is never replaced merely for this presentation change.
4. [ ] **INT-004 — Startup retry** — Disable networking during startup, wait for the timeout/error, restore networking, and tap Retry. → **Expected:** The error explains recovery, retained content is not discarded, and retry reaches the usable app without relaunching. Inspect the startup recovery pane at 200% text, RTL, short landscape and tablet width: the native-safe message, optional Retry and diagnostic Copy remain reachable. Copy preserves the exact safe report. Only explicit Retry restores loading and re-awaits the process-owned attempt; rotating/re-attaching keeps an actionable failure visible. No profile chooser may bypass native bootstrap readiness, activate retained fixture profiles or claim recovery success.
5. [ ] **INT-005 — Warm resume** — Open a conversation, background the app for 30 seconds, and resume it from Recents. → **Expected:** The same account and useful surface return without a second splash, duplicate navigation, or stale overlay.
6. [ ] **INT-006 — Process recreation** — With a conversation open, use the developer option “Don't keep activities” or kill the process, then reopen. → **Expected:** The app restores or safely falls back to the correct account and chats; the first restored chat-list frame uses the selected titles and avatars, never flashes “Unknown,” and never shows another account's messages.
7. [ ] **INT-007 — Offline shell** — With populated local data, disable all networks and relaunch. → **Expected:** Existing chats/messages remain readable, an offline/retrying state is honest, and network-only actions fail safely without deleting local content.
8. [ ] **INT-008 — Connectivity recovery** — While the offline shell is visible, restore networking and receive/send a synthetic message. → **Expected:** Connection indicators recover without an app restart and the new message appears once.
9. [ ] **INT-009 — Recents privacy** — Open a conversation, enter Android Recents, and inspect the task preview. → **Expected:** The preview is protected according to Device privacy settings and does not expose chat content when screenshots are blocked.
10. [ ] **INT-010 — Upgrade with retained data** — Take a private backup and record the installed version, then install the candidate in place over the previous release containing prepared private-key and Amber accounts/chats, without uninstalling or clearing storage. Open each account, check its relay settings, send and receive a synthetic message, then force-stop and reopen twice. For a MarmotKit SDK update, also run `ONB-006`, `ONB-008`, `ONB-012`, `ONB-017`, `ONB-018`, `ONB-020`, `SEC-007`, and `SET-019` to cover fresh sign-in, recovery, signer integration, and build identity. → **Expected:** Both account types reach usable chats without a startup or migration error; relay settings, drafts, folders, and message history remain available; messages appear once; and migrations do not repeat or reset user choices on subsequent launches. MarmotKit 0.9.20 advances account migrations 65–67 and shared storage 2–3; keep the pre-upgrade backup because downgrade is unsupported. Previous telemetry consent requires a fresh choice as covered by `SEC-007`. MarmotKit 0.10.2 advances account migrations 81–86 (attachment history, deletion provenance, retained attachment acquisition, worker demand, protected partial downloads and durable attachment controls): downgrading a migrated account is unsupported, and the app offers no way back — its encrypted backup exports an `ncryptsec` secret key, not the account database, and `ACC-012` expects even that import to fail, so a migrated install can only be moved forward or wiped. Keep a device-level copy of the app data before upgrading if the populated install must survive. An upgrade that crosses migration 47 also needs at least 3.25 times the account database size free before it starts. MarmotKit 0.10.0 advances account migrations 70–75 (forgotten groups, reset cutoffs, chat-list paging, draft revisions, invitation attention and user blocks): after an in-place upgrade from a 0.9.21 install, run this case on an emulator first, then confirm the chat list, an open conversation, an unsent draft and the Blocked users list all load without a repeated migration, and cover the new surfaces with `CHL-005`, `CHL-008`, `CON-002`, `CON-008`, `MED-017`, `ACC-002` and `SEC-014`. MarmotKit 0.10.1 advances account migrations 76–80 (accepted edits, avatar storage and acquisition, content reports, avatar target lookup) and carries the fix for conversation windows that waited on live hydration: after an in-place upgrade from a 0.10.0 install, confirm a fresh DM, a newly created group and an existing group all open without the "still finishing" state persisting, then cover the new surfaces with `MSG-007`, `MSG-016`, `CHL-017` and `GRP-023`. An install that still predates account migration 47 needs roughly 3.25× the account database's size free on the device before the upgrade starts; check free storage first on such fixtures.
    - **INT-010 cross-preview subcase:** On a preview channel whose builds share package identity and app data, populate a MarmotKit 0.9.21 build, install and launch one 0.10.0 preview in place so migrations 70–75 complete, then force-stop it and install this candidate from a different preview branch without uninstalling or clearing storage. Launch once offline and once online. → **Expected:** The candidate retains the 0.10.0-compatible runtime, opens the existing accounts and chats without an `APP_BOOTSTRAP`/`RUNTIME_START` failure, preserves the draft and history, and does not rerun or reverse the migrations.

11. [ ] **INT-011 — Avatar recovery** — With retained profile and group metadata, turn off Wi-Fi and mobile data, force-stop the app without clearing its data, and reopen. Keep the missing profile/group avatars visible, then restore networking. Repeat while the app is backgrounded, return to it, and switch accounts while an avatar is loading. → **Expected:** Missing visible avatars retry when validated internet returns, without restarting the app, navigating away, or waiting for the previous failure cooldown. A backgrounded screen waits until visible before retrying. Already-loaded pictures do not flash back to initials; a late old-account image never replaces the selected identity. A genuine remote image failure keeps the fallback without a polling loop. This check does not promise offline image persistence after process death.

### Onboarding, sign-in, and recovery

1. [ ] **ONB-001 — Landing screen** — Open Welcome in portrait, landscape, tablet, RTL and at 200% text, with and without retained accounts. → **Expected:** The monochrome mark centers above a maximum 520 dp action column, Sign In is outlined and Sign Up filled; retained accounts add Continue and Choose profile. Actions remain inside safe areas and scroll when space is short; an outgoing IME does not lift Welcome. Visiting or choosing a sign-in screen creates no identity. Native Sign Up/Sign In/setup errors remain visible and copyable above pinned actions. After a publication failure, tap the actual Retry button with the error still shown; verify it retries publication only. Check the same separation with an open IME, 200% text and setup recovery actions; consumed ancestor IME insets must not be subtracted twice.
2. [ ] **ONB-002 — Offline sign-up** — Disable networking, open Sign Up from Welcome, edit Name/About and choose a local photo, then submit. → **Expected:** Opening/editing/selecting only stages a local draft. Submit and Retry show the existing offline notice and never create an identity, upload a photo or publish metadata. Back before submission returns to Welcome and creates nothing; 200% text/RTL/IME keep fields and the pinned action reachable.
3. [ ] **ONB-003 — Create identity and publish profile** — Using an explicitly designated disposable identity, enter Name/About/photo, restore networking and submit. Repeat taps during each busy stage. → **Expected:** One accepted native identity is used throughout; the UI names creation, photo upload and profile publication accurately and disables competing edits/Back while busy. Simulate upload failure and metadata failure separately, then Retry: accepted account and submitted fields remain; metadata retry reuses an uploaded photo URL and neither retry creates another identity. Failure copy distinguishes created identity from incomplete profile publication. Continue without these changes, or Back then its confirmation, retains the created identity without claiming metadata success; Cancel on that confirmation keeps the draft. Another account/runtime selected during creation/upload/publication is never activated, published or marked ready by stale completion. If the active account changes during sign-up, the error says the tester switched accounts, tells them to close the form and choose a profile, and reassures them that a profile already created is still available. A late photo/file/web result after Back, replacement or Submit is ignored. Native private-key checklist/recovery consent and deferred usage consent remain unchanged. On the Welcome/Add Profile entry, Sign In and Sign Up stay at the bottom above system navigation in light/dark modes and short windows; both remain reachable with large text.
4. [ ] **ONB-004 — Open login and Back** — From Welcome tap Sign In, type synthetic text, then use toolbar and system Back. Repeat while a controlled import or Amber request is pending. → **Expected:** A masked Private key field, paste/clear action and separate QR button appear beneath the Sign In top bar; Back returns to Welcome without import while idle and is consumed while busy. The pinned Amber/Sign In actions remain above the IME and the form scrolls.
5. [ ] **ONB-005 — Empty and malformed key** — Submit an empty value, an `npub`, random text, and a checksum-invalid `nsec`. → **Expected:** Each is rejected inline with actionable text; no account is added and the app stays responsive. Paste and scan a synthetic secret, then scan a public/unrelated QR. → **Entry expected:** Clipboard reads occur only after Paste; camera/clipboard fill masked text and never import automatically. Public/wrong QR leaves the old key unchanged and shows a dialog plus inline error. Scan remains available after text entry; a late scanner result cannot edit a busy or disposed form. No raw key appears on screen or in task-switcher capture.
6. [ ] **ONB-006 — Valid private-key sign-in** — Paste Alice's disposable valid `nsec` and submit. → **Expected:** Progress is visible, the submitted key and sensitive clipboard are cleared after identity acceptance, and Alice's native setup checklist opens before Chats. Complete `ONB-013` through `ONB-021`. Scan a disposable private-key QR through the sign-in field: the camera permission card explains private-key scanning, the result only fills the field, and import waits for explicit Sign In. Close, wrong/public QR and late results after dismissal or a busy operation must not submit or replace an accepted key.
7. [ ] **ONB-007 — Sign-in failure and retry** — Interrupt the network after submitting a valid disposable key, wait for failure, restore it, and retry. → **Expected:** The error distinguishes retry from invalid input and retry succeeds without duplicate accounts.
8. [ ] **ONB-008 — Setup recovery consent** — Sign in with a fixture whose remote setup requires recovery, read the warning, cancel once, then confirm. → **Expected:** The warning plainly states that unfinished setup is cleared and sign-in is retried, while invitation keys already published may remain on relays. Cancel leaves setup unchanged; confirm runs one recovery attempt and reports success or a specific failure without repeatedly prompting.
9. [ ] **ONB-009 — Saved-account continuation** — Sign out without wiping a retained test account, return to onboarding, and tap its saved-account action. → **Expected:** The named account shows a single reactivation progress state and returns to that account. With two retained profiles, open Choose profile and select the second. → **Expected:** The exact selected profile resumes, only it shows progress, and no new identity or draft is created.
10. [ ] **ONB-010 — Amber absent** — Use a device without a NIP-55 signer and open Welcome → Sign In. → **Expected:** Sign in with Amber is absent; ordinary private-key sign-in remains available.
11. [ ] **ONB-011 — Amber present and cancelled** — Open Welcome → Sign In on a device with Amber, tap Sign in with Amber, then cancel in Amber. → **Expected:** Only Sign In offers the signer action, real request/proof stage text explains each wait, all conflicting actions are disabled while busy, and cancellation returns to the usable Sign In form without creating an account.
12. [ ] **ONB-012 — Amber success/error** — Complete disposable Amber login, then repeat with a denied or failed signer response. → **Expected:** Success opens setup for the matching account before Chats; denial/failure shows a recoverable result without exposing key material or trapping the UI.


13. [ ] **ONB-013 — Imported-account setup checklist** — Import disposable private-key and Amber accounts with healthy metadata and missing metadata. → **Expected:** The full native checklist shows each real waiting/checking/passed/skipped/attention status. The current idle checkpoint opens a detail destination even when no action is offered, so saved-operation evidence stays inspectable; completed or future rows grant nothing. Open Chats is disabled until native readiness without pending cancellation. Healthy profile checks, optional-follow skipping without publication, and safe defaults for confirmed-missing relay lists retain their native behavior. Open the Profile row to edit or skip a missing name. Saved cancellation and approved-publication notices also remain visible on the checklist. Top/system Back from a detail returns to the checklist without acknowledging a checkpoint; checklist Back leaves setup. Test 200% fonts, RTL, short landscape, tablet and IME: the 520 dp column, scrollable content and pinned action region stay reachable.
14. [ ] **ONB-014 — Optional name setup and Amber approval** — Import a fresh disposable identity. Choose Edit profile, enter a name, review it, and approve publication. In a separate run choose Not now and verify setup continues. Also resume an unapproved saved profile proposal, discard once, then approve a new edited draft. Repeat with a disposable Amber account and deny its profile-signing prompt once before retrying. → **Expected:** A restored user-authored proposal never becomes an automatic default. Other profile fields remain unchanged; failed drafts remain editable; denial leaves a recoverable screen without a prompt loop. Fresh imports with missing profile details offer editing or skipping; healthy existing profiles pass automatically. Shared fields stage local edits. Save flushes the last IME text into the native review proposal before approval; name/About changes retain all other metadata. Rotate or recreate the view during editing, then Cancel: no publication occurs. Switching account/runtime must not carry field buffers or submit a stale proposal.
15. [ ] **ONB-015 — Discovery failure and retry** — With an unreadable or unreachable relay fixture, inspect the finding, retry, and enter another discovery relay. → **Expected:** Findings distinguish invalid, unsafe, retired, restricted, unreachable, and timed-out endpoints. The current finding names its affected relay address directly on the decision screen, without opening Details; findings with no endpoint show only their explanation. Discovery-only changes publish nothing. An inconclusive lookup offers retry/discovery rather than silently publishing defaults. Open the current Relay or Inbox checklist row to inspect the native finding; Back returns to the checklist without a retry or proposal.
16. [ ] **ONB-016 — Destructive relay replacement review** — Import a fixture with a broken relay entry that requires cleanup. Choose recommended or custom relays, inspect the full proposed list, discard once, then approve. → **Expected:** Removing or replacing existing entries requires explicit consent. The concise warning explains that omitted relays are removed and other Nostr apps are affected. Read/write capabilities and exact inbox endpoints match the reviewed proposal. Inbox and discovery editors retain distinct labels. A proposal from another step never appears under the current decision. Discard does not automatically recreate or publish the proposal.
17. [ ] **ONB-017 — Stale proposal and interrupted publication** — Leave a proposal visible while another controlled test installation changes that relay record; attempt approval. Separately interrupt an already approved publication and resume. With a controlled recovered-checkpoint fixture, change the recovery attempt while keeping its proposal revision unchanged, then try the previously displayed approval and review the refreshed proposal. → **Expected:** A changed decision or recovery attempt requires fresh review; a prior approval cannot authorize the replacement attempt, even when its revision is identical. An unapproved restored proposal stays unapproved; an approved interrupted publication retries its saved operation without requesting a replacement proposal or claiming the earlier publication can be undone.
18. [ ] **ONB-018 — One-device acknowledgment** — Open the current Device checklist row with fixtures for no discovered packages, another possible installation and incomplete discovery. Read the notice and choose Use this device. Replace a recovered attempt at the same revision before tapping, then review the refreshed notice. → **Expected:** Exact native discovery evidence and Details remain truthful; no device is claimed online or absent. Only explicit consent for the current recovery epoch allows initial messaging-key publication. Routine detail has Use this device and Details without Retry or Finish later. Detail Back returns to the checklist without consent; checklist Back saves and exits. Genuine failures explain Retry and Finish later. Native certified completion still activates Chats automatically; no synthetic checkpoint or extra mandatory completion tap is introduced.
19. [ ] **ONB-019 — Later and restart** — During checks and while a decision is pending, use Finish later or system Back, select the saved account to resume, rotate, then force-stop and relaunch. Repeat after returning from Amber. → **Expected:** MDK's saved progress resumes without repeating completed repairs, losing an unapproved proposal, switching to the wrong account, or trapping the screen in a spinner. Pending setup does not open Chats or ordinary push/maintenance work. Completing resumed setup restores notification-receiver readiness and privacy settings before Chats opens. On the new routes, idle editor Back cancels only the editor, idle detail Back returns to checklist, and checklist Back/Finish later uses the existing save-and-exit owner. Back during native work retains the existing save-and-exit/cancellation owner. A newly advanced native decision returns to the checklist and requires opening its own row.
20. [ ] **ONB-020 — Add account and cancel** — From a populated account use Add account to import another disposable nsec or Amber identity. Leave setup for later, resume, open Details, and cancel at the device notice. → **Expected:** Defaults run without activating the pending account; leaving returns to the original usable account. Cancel retains the new identity signed out and completed publications. Repeat cancellation during an approved or interrupted publication and while Amber holds a signing request, then complete the late response and restart the app. The cancelled attempt stays signed out and cannot be revived by that response. A later explicit sign-in starts a fresh attempt while MDK retains evidence of uncertain publications; cancellation never claims to undo a publication. Before native entry, sign-out, wipe, account/runtime replacement or retained-account reactivation revokes an already-captured Add Profile callback. The shared Sign Up owner retains a native creation receipt before activation and refuses activation/publication if its captured account/runtime changed; the receipt remains recoverable through ordinary account re-entry. Closing an unsubmitted form invalidates its old callbacks. Existing import/Amber work remains process-owned after acceptance; local dismissal alone is not setup cancellation, rollback, or authorization for recovery. Use the existing explicit setup cancellation/consent paths for the checkpoint cases above.
21. [ ] **ONB-021 — Direct completion and recovery** — Open the current decision row and tap its action twice; reconnect Amber after denial/unavailability. Deny a relay-default signature once and retry explicitly. Interrupt final activation and retry when available. → **Expected:** One native operation starts per tap sequence. Signer rejection restores explicit recovery without a prompt loop. Once certified ready, fresh native verification activates Chats automatically. If activation fails, the checklist retains a working Open Chats retry with native readiness rechecked; cancelled/unfinished checkpoints never enable it. Returning to a checklist or rotating never acknowledges a step.
22. [ ] **ONB-022 — Offline identity acceptance** — Disable networking, submit a new disposable nsec, and then restore the connection and resume checks. Separately try Sign up while offline. → **Expected:** nsec acceptance clears the key and opens saved/retryable preflight without requiring network validation. Sign up retains its existing offline notice and retry behavior.
23. [ ] **ONB-023 — Defaults preserve existing relay lists** — Import disposable accounts with healthy existing lists, a confirmed-missing list, and a read-only list with no write route. Repeat with incomplete discovery. → **Expected:** Healthy lists pass unchanged; confirmed-missing lists receive defaults without a confirmation tap. Existing lists that need repair stop for review before any replacement is published. Incomplete discovery never authorizes automatic defaults or removal. If a record appears between discovery and approval, setup stops for fresh review.
24. [ ] **ONB-024 — Unreadable saved-checkpoint recovery** — Open onboarding with a disposable saved profile whose setup checkpoint is unreadable or exhausted. Also simulate a transient failure while reading its recovery state at launch and from the account picker, then retry after the read succeeds. Tap its Recover sign-in setup action, cancel once, then confirm and enter that profile’s private key again. → **Expected:** A failed recovery-state read fails closed: the account stays out of Chats and ordinary background work instead of being treated as ready, and a later successful read routes to the correct saved setup or recovery surface. An unreadable checkpoint is not treated as ordinary resumable setup. Confirmation explains that the profile is signed out, unfinished setup is cleared, the private key is needed to sign in again, existing relay publications stay, and recovery publishes nothing new. Cancel changes nothing; confirm acknowledges the latest evidence once, retires the unreadable attempt, and requires a fresh sign-in without exposing or retaining the private key.

25. [ ] **ONB-025 — Random profile name dice** — On Sign Up, imported-account setup → Edit profile, and Settings → Profile → Edit, tap the dice beside the name twice, then type a custom name. Repeat with an empty name, a busy signup/setup editor, RTL and 200% text. → **Expected:** Each enabled tap proposes a two-word name from the existing generator, replacing only the current draft name and retrying when the generator repeats the name already shown; typing remains possible. The dice is visible but disabled during busy stages. No name is published until that screen's normal Submit, review/approval, or Save. Leaving or cancelling keeps the previously published name, and Edit Profile's separate Restore saved name action still works.

### App shell, accounts, and navigation

1. [ ] **NAV-001 — Chats to Settings** — From Chats open Settings, then use the top back arrow and Android Back in separate visits. → **Expected:** Both return to Chats rather than exiting the app.
2. [ ] **NAV-002 — Settings detail hierarchy** — Open Appearance, then Action color; press Back twice. Repeat Help → Report a bug and Help → About & licenses. → **Expected:** Back returns through the immediate parent (Action color → Appearance → Settings; Report a bug or About & licenses → Help → Settings) and never skips a level or leaves the app.
3. [ ] **NAV-003 — Settings scroll retention** — Scroll Settings near the bottom, open a detail, and return. → **Expected:** The prior Settings position is restored.
4. [ ] **NAV-004 — Account selector** — Open the account selector from Chats and from Settings → Switch Profile (shown with several signed-in profiles), dismiss by swipe/outside tap, then reopen. → **Expected:** Both entry points show the same accounts and active selection; dismissal changes nothing. With one signed-in signing account the Chats avatar opens Settings; with several it opens this native selector, including unread counts and Add account. Retained signed-out entries never earn an avatar beside the active one. Direct switching and unread accessibility also live in the shared profile sheet. Chats shows the 40 dp active avatar, with the other accounts stacked beside it only while Quick account switching is on. With several eligible profiles, open the shared active-first sheet and choose any native row directly; its inactive badge uses only confirmed unread for that profile, including a manual-unread dot at zero messages. Verify retained signed-out/read-only labels and their existing recovery/selection behavior. Add Profile retains native create/import/Amber entry; Settings closes the sheet before the existing hub route. Closing/Back never activates a pending row. In LTR and RTL, including AMOLED, the active avatar's unread marker remains fully inside the circular button edge instead of being clipped.
5. [ ] **NAV-005 — Switch account** — Switch Alice → Bob from the selector, then return to Chats. → **Expected:** Profile, chats, unread counts, drafts, folders, settings, and notifications all belong to Bob; no Alice content flashes. With the default Quick account switching preference enabled, use the other accounts shown, visibly separated, beside the Chats avatar. Tap one to land on that account directly; long-press opens the full selector. Each carries its own unread dot, up to three avatars plus a +N chip. With four signed-in profiles on a normal phone-width chat list, the three quick avatars are visibly separated rather than stacked: tap the centre and both edges of the second and third and confirm each reaches only its own profile, in LTR and RTL and with an unread dot present. Repeat at the narrowest supported width and at 200% text, where the bar shows fewer quick avatars and collapses the rest into the +N chip instead of compressing them; the chip opens the full switcher, and the stack never covers the active-profile button, the update emblem or search. A native short toast names the actual activated account only after success. Failed or removed targets produce no false switched confirmation. Header actions are the real update emblem and search; the row remains ON by default and keeps stable native order independently of the active-first selector display. Existing search/voice and conditional connectivity status remain available. Scroll, restore and return to top to verify the surface-to-surfaceContainer header transition. With two accounts that share a group, scroll Alice's chat list well past the top — far enough for the jump-to-top control to appear — before switching from the stacked avatars: Bob's list opens at its first row with no jump-to-top control, and an open bulk-delete confirmation from Alice is gone. Repeat A → B → A, and repeat with the Archived filter selected. Within one account, opening a chat and coming back still restores the position you left.
6. [ ] **NAV-006 — Rapid account switching** — Switch Alice → Bob → Alice while lists are loading. → **Expected:** The final UI binds to Alice, stale Bob results do not replace it, selected chat titles and avatars appear on the first frame without an “Unknown” flash, and controls do not remain busy. Repeat with the stacked account avatars, a target removed/signed out during activation, and failed local reads. Existing activation fences reject superseded targets; no wrong identity frame or stale completion toast appears. Repeat TalkBack, keyboard, 200% text/RTL and AMOLED; the stack keeps a 48 dp row height, one named accessibility action per account, and mirrors its overlap in RTL. Hold native selection, repeat the same row, select a newer target, close/dispose, remove target or begin sign-out/wipe. Only an admitted current native activation may dismiss/navigate. Recheck per-account chat draft/history isolation, keyboard/TalkBack direct rows, long names, 200% RTL and AMOLED. The list scrolls while Add Profile/Settings stay below it. With three signed-in profiles whose avatars have all been displayed at least once, tap a quick-switch avatar and watch the remaining chips through the switch: each keeps the name and avatar it was already showing, with no blank circle, initials placeholder or visible reload between the old account's last frame and the new account's first one. Repeat in light, dark and AMOLED, and once with a profile that has no locally stored picture, which keeps its stable generated placeholder instead.
7. [ ] **NAV-007 — Add account from selector** — Tap Add account, import Carol's disposable account, and finish. → **Expected:** Carol is added once and becomes available in the selector without removing Alice or Bob. Repeat Add Profile from Settings and the selector. Double-tap or replay an already-captured Sign Up/import callback: one form or native import starts. Duplicate form Submit starts only one creation. After form Back or an account change, an old captured Submit cannot revive that form or adopt the new account. Back from idle Sign In clears the local key and returns to Welcome; Back from Welcome closes the flow. During import/Amber work, system Back retains local dismissal without claiming rollback. Shared Sign Up blocks Back during native create/upload/publication and offers explicit Continue without profile only for its supported failed stages. Reopen before a late failure returns: that old result must not dismiss or contaminate the new entry. Amber failure or cancellation remains visible above the secure entry and diagnostic Copy preserves the exact safe report when offered. Shared Sign Up displays actual creation/profile-stage failure and preserves stage-specific retry; it never labels an uncertain or failed publication complete. During a held import, dismiss/reopen and copy different text before native success: that replacement clip remains unchanged; only a single plaintext clip still matching the submitted key is cleared on acceptance.
8. [ ] **NAV-008 — Conversation return** — Open a chat, inspect details/profile/media, and unwind each route with Back. → **Expected:** Each route returns to the originating conversation and preserves its transcript position and draft.
9. [ ] **NAV-009 — Rotation and recreation** — Rotate on Chats, Settings detail, a modal sheet, and a conversation with a draft. → **Expected:** Content remains within safe bounds; durable state is retained and transient dialogs either restore safely or dismiss without acting.
10. [ ] **NAV-010 — External return** — Open a browser/store/system-settings action from White Noise and return with Android Back/Recents. → **Expected:** The correct White Noise account and prior screen resume once.

### Chat list, folders, and chat actions

1. [ ] **CHL-001 — Empty chat list** — Open Chats for a new account with no conversations. → **Expected:** A clear empty state and New message action appear; no stale rows from another account are visible. Check the new, empty selected folder, unread-only, archived-only and search-empty views at 200% font and RTL. Each uses its own title/detail; the first-chat callback still reaches the existing New chat flow, and no failed/loading condition is relabelled empty. When ordinary Chats has conversations but none have ended membership, Left shows its own empty state without a New message action. Loading/failure states retain priority over this empty claim. Selection uses the Select chats toolbar and pinned More/count/Select All controls. Check compact, landscape, RTL and 2x text; count is spoken with selection context. Single-chat read/unread, mute, pin and manual pin order stay conditional native actions; mixed bulk archive/delete retain their existing scope and confirmation. Close or switch accounts before a captured action runs: no action can reacquire the replacement selection.
2. [ ] **CHL-002 — Populated list** — Open a prepared account. Send a text and immediately press Back while its pending clock is still visible. → **Expected:** Direct/group rows show the correct title/avatar, last-message preview, time, delivery/unread state, typing/draft indicators when applicable, and stable ordering. Check the native compact rows: 52 dp avatar, 20 dp pin badge, title-adjacent muted/timer/ended markers, a one-line ellipsised preview, and exact typed attachment/status metadata. Rows grow for large fonts or a two-line search snippet rather than clipping. Your own latest message carries the bubble's own delivery glyph beside the row's badges — the pending clock while sending, the filled check disc once sent, the error badge once a send has actually failed, which needs a failed send to observe rather than the ordering check below — and an incoming latest message carries none. After returning during a pending send, the chat-list row changes to the filled sent disc as soon as the authoritative delivered projection arrives, without reopening the conversation or showing the wrong state for an identical text. On a long list, scrolling away from the first and last rows adds the same soft theme-independent edge fade used by the conversation transcript: only the bottom fades at the start, both edges fade in the middle, only the top fades at the end, and a short non-scrollable list remains fully opaque. The mask affects list content only; search, folders, overlays, FAB, snackbar, row taps and long-press actions remain clear and interactive. Removed, voluntarily left, pending leave and disbanded states retain distinct descriptions; real unread/mention and send failure precedence remain intact. Repeat AMOLED, large fonts and RTL.
3. [ ] **CHL-003 — Incoming row update** — Have Bob send Alice a message while Alice remains on Chats. → **Expected:** The matching row updates once, moves according to recency, and unread state appears without losing scroll unexpectedly.
4. [ ] **CHL-004 — Native row preview** — Type but do not send in a chat, return to Chats, reopen the chat, then send. Exercise native Message, Invitation and Empty preview fixtures while an obsolete host draft is present. → **Expected:** MarmotKit's selected Draft preview appears, the exact draft restores, and the marker clears after send. Message, Invitation and Empty suppress the obsolete host draft and render their authoritative localized row state without independently re-inferring the selected preview.
5. [ ] **CHL-005 — Open and return position** — Scroll a long chat list, open a row, then Back. Repeat on the first conversation open after force-stop without clearing storage, both with and without a pending forward request, and with a prepared public-event card. Use an account with more than 200 conversations for one run: scroll far enough that the bounded window evicts its oldest rows, then scroll back toward the top and use the jump-to-top control. → **Expected:** The list returns near the prior viewport and the opened conversation is correct. First opening stays responsive while event-card networking and encrypted forward restoration initialize; pending selections restore without loss. In a debug build, no main-thread StrictMode violation originates from event-query SSL initialization or pending-forward storage initialization. Repeat deep scroll, opening history and Back in Left. Left never supplies an ordinary active-list head for return reordering. Change scope during selection and confirm hidden rows are not retained for bulk actions; repeat query changes and native membership updates so search never displays a previous scope corpus. The list keeps loading rows in either direction at a retained edge, the row being read stays in place across replacements, pinned and unpinned conversations do not disappear, and jump-to-top remains available until the true first row is restored.
6. [ ] **CHL-006 — Pin/unpin** — Long-press a chat, pin it, then unpin it. → **Expected:** Pinned state and ordering update consistently and survive relaunch. The row context menu is anchored with 8dp spacing and highlights its row without selecting it. Hold until commands appear, then drag through adjacent rows before release: the original native range gesture remains active and no conversation opens. Stationary release transfers focus to the popup. Close and reopen the same row quickly: old command, dismissal and pointer callbacks must not affect the replacement opening. Repeat with TalkBack long-click, Back, and outside dismissal; scrolling the anchor out of composition or leaving Chats dismisses its menu.
7. [ ] **CHL-007 — Archive/unarchive** — Archive a chat, open the archived-only folder/view, then unarchive it. → **Expected:** The chat leaves the main list, appears in Archived, and returns to the main list after unarchive.
8. [ ] **CHL-008 — Mark read/unread** — Mark a read chat unread, open it, then return. Also switch to an account with a pending group invitation. → **Expected:** Unread decoration/count appears, then clears after the conversation is read; the account switcher badge counts unread messages plus conversations that only need attention (a pending invitation counts once), and while MarmotKit is still preparing a summary the badge shows the last known count as provisional rather than zero.
9. [ ] **CHL-009 — Mute shortcut** — Mute a chat from its list action, choose a duration, then unmute. → **Expected:** Mute state is visible, the chosen expiry is represented correctly, and unmute restores notification eligibility.
10. [ ] **CHL-010 — Delete chat confirmation** — Invoke Delete on a disposable chat, cancel, then confirm. → **Expected:** Cancel preserves the chat; confirm removes only that chat and reports failure without hiding it if deletion fails. Repeat Read/Unread, Pin/Unpin and native manual pinned moves from the anchored menu. Commands retain native membership/archive eligibility; no unavailable action is synthesized. Changing account/runtime or starting sign-out/wipe between presentation and dispatch must prevent the stale action.
11. [ ] **CHL-011 — Multi-selection** — Long-press one row, drag or tap to select multiple visible chats, deselect one, then exit selection with Back. → **Expected:** Count/actions match selected rows and Back clears selection before leaving Chats. A stationary hold opens actions once without also opening the chat on release; continuing vertically enters range selection and survives the row changing into selection mode. A selection-mode tap toggles once, a stationary selection hold does nothing, and disposal/disabled transition cannot leave a live drag or trigger a stale open. The leading checkbox has one row-level selection announcement. Verify the anchored menu retains Folder, Archive/Unarchive, Mute/Unmute and Select with their existing native targets. Test light/dark/AMOLED, high density, RTL, 200% text and short landscape: overflow scrolls and every command remains reachable.
12. [ ] **CHL-012 — Bulk actions and pinned boundary** — Apply each available bulk action to disposable selected chats, including mixed pinned/unpinned rows. Change MarmotKit's advisory read/unread, pin/unpin, mute/unmute, archive/restore and delete-local capabilities while reopening the row menu. → **Expected:** Only native-permitted commands are offered, mutually exclusive capability pairs follow the current row snapshot, only selected rows change, disabled combinations are explained, and list ordering remains valid. Delete from a row menu still dismisses commands and enters the shared destructive confirmation. Cancel retains canonical history; confirm targets only that captured native row. `canStartLeave` remains advisory to the existing authoritative leave preflight; prototype-only Leave is not added to this action owner because production ChatActionSheet has no corresponding callback, and the existing native leave/recovery flow remains unchanged.
13. [ ] **FLD-001 — Folder filter** — Create or rename a folder with a long name and at least two eligible chats. Select every folder pill on Chats and return to Chats, then open Forward on a disposable message and horizontally scroll the compact folder-chip row with TalkBack enabled. → **Expected:** Chat filtering follows each folder's rules, the selected chip is clear, and Chats restores the complete active list. Forwarding chips stay in one row without hiding destinations; a long visible label shortens cleanly, while TalkBack announces its full name, eligible-chat count, and selection state from one comfortable tap target. If folder preferences change while Forward remains open, names and eligible counts refresh without reopening; folders with fewer than two eligible destinations disappear. Folder navigation uses circle pills with the permanent Chats reset, native visible folder order/counts, and Manage folders. The selected pill scrolls into view on restoration/reorder/filter change; selecting it again never clears it. Long press/TalkBack Edit uses the exact native folder ID and current callback. Manage opens the existing folder screen, defers diagnostics while open and preserves the selected filter on Back, including an empty list. Repeat RTL, 200% text and AMOLED/scroll tint. Native archived-only source and nonselected-empty visibility rules are unchanged.
14. [ ] **FLD-002 — Folder changes while selected** — Create a named keyword folder with at least three eligible matching chats, including Bob's direct chat. In separate runs, keep that folder selected on Chats and keep a message's Forward picker open while Bob changes the matched disposable profile name so that chat leaves or rejoins the rule; also edit manual membership and return to Chats. → **Expected:** Folder rows update without duplicated chats or a stale empty state. The open Forward picker stays visible as its eligible count changes between three and two and refreshes its Off, mixed, or On state without reopening. A folder with fewer than two eligible forwarding destinations is omitted, and no folder membership or selection is borrowed from another account. Add to folder opens a multi-folder manual-membership draft: mixed means only some selected chats are manual members, and rule inclusion remains separately described. Checkbox taps do not persist before Save; Save changes only touched manual memberships and retains unrelated chats/rules. Rotation preserves pending intent for its original account/runtime/chat set. Cancel or New folder abandons pending picker choices without saving; the native New folder editor still receives selected chats. Removing a touched folder during editing produces a retained failure and no false success; Cancel allows reopening against current folders.
15. [ ] **CHL-015 — Partial load failure** — With cached rows present, interrupt relay access and refresh. Also open the app on an account MarmotKit is still finishing after an upgrade (INT-010). → **Expected:** Existing rows remain usable and a non-blocking error/retry indication appears; a full-screen error is used only when no useful rows exist. If MarmotKit refuses the bounded chat-list windows, the list still renders and stays live from the whole presented list, and the copyable report names the engine error (`marmot=…`). While MarmotKit reports a not-ready state the message says the update is still finishing and retries every couple of seconds rather than backing off to minutes. When no rows are cached and the first window has not arrived after four seconds, the bare spinner gains the same still-finishing copy and the release log carries one `chat_list_first_window_slow` marker; a chat row whose group MarmotKit no longer knows is read once and never retried.
16. [ ] **CHL-016 — Presented chat-list projection** — Prepare a DM with a contact nickname, a named group, an unnamed group, a missing presentation, and remote/encrypted/placeholder avatars. Change a title or avatar, then separately change unread, pin, archive, and row order without changing the presentation. Rapidly switch accounts and background/resume during updates. → **Expected:** Each row uses MDK’s selected title/avatar and typed unnamed/unavailable fallback; row-only changes still update immediately. Complete snapshots replace the prior list in sequence, while stale generation, sequence, or account-store updates never restore old rows or mix accounts.
17. [ ] **CHL-017 — Durable avatars and projected system previews** — With avatars already shown once, turn off Wi-Fi and mobile data, force-stop the app and reopen. Then, online, change a contact's profile picture and a group's avatar from another device, and let the newest activity in a group be a membership or name change. → **Expected:** Chat rows, the conversation header and sender avatars beside incoming bubbles draw from MarmotKit's stored avatar bytes and stay visible offline after a cold start, without a fetch or an initials flash; a changed picture replaces the old one once the engine reports the new revision and never renders from the previous bytes. Rows whose avatar the engine does not hold yet fall back to the profile URL exactly as before and fill in once acquisition completes. A group's system-event preview in the chat list reads the engine's projected event, so a member-authored payload can never pose as an authenticated change, and shows no raw JSON.

### Search and starting conversations

1. [ ] **FIND-001 — Open/close search** — Tap Search, type text, then press Back. → **Expected:** The field gains focus and keyboard; Back closes any filter sheet first, then search, before it can exit the app. Verify the compact 48 dp-minimum search field: voice is available for an empty query, typing replaces it with Clear, Clear retains focus and restores empty state, and Back uses the existing shell transition. Check 200% text and RTL; normal profile-cycle/header actions remain unchanged.
2. [ ] **FIND-002 — Search titles and profiles** — Search by direct-contact display name, group title, and case/diacritic variations. → **Expected:** Matching sections/rows appear without duplicates and unrelated rows stay hidden.
3. [ ] **FIND-003 — Search message bodies** — Search for a unique word in an older message and tap the result. → **Expected:** The owning chat opens and scrolls/highlights or otherwise identifies the matching message. The list row displays that older matched message’s highlighted snippet and timestamp rather than the newest message’s preview/time after restyling. The current native contract returns the existing per-chat body match, highlighted snippet, timestamp and exact message navigation target. Prototype full message bubbles/account-wide paging are not inferred from that smaller result. In-conversation search uses the Messages field; Enter/Search advances using the existing match owner, Clear restores the original query behavior, and Close returns to the transcript. The date-jump icon is visibly disabled until that capability exists.
4. [ ] **FIND-004 — Search no results** — Enter a unique nonexistent term, then clear it. → **Expected:** An explicit no-results state appears and clearing restores the prior list.
5. [ ] **FIND-005 — Valid `npub` search** — Paste Bob's valid `npub` into search and choose the resolved profile/action. → **Expected:** Bob resolves locally and the correct profile/direct-chat path opens. The resolved-identifier profile row is one native action carrying the full resolved npub; a known canonical DM still opens that existing chat through its original owner.
6. [ ] **FIND-006 — Invalid `npub` search** — Paste a checksum-invalid `npub`. → **Expected:** A specific invalid-identifier result appears and no conversation starts.
7. [ ] **FIND-007 — NIP-05 search** — Search a controlled valid NIP-05 address, then a nonexistent address. → **Expected:** A bounded resolving state leads to the correct profile for the valid address and an explicit not-found result for the invalid one.
8. [ ] **FIND-008 — Search filters** — Open filters and exercise each displayed type, chat/person, media/content, and date option individually and in a supported combination. → **Expected:** Applied filters are summarized, results satisfy all selected filters, and Clear removes them. Current production intentionally hides typed Date/Content filter entry because the native query does not execute these predicates; mark that entry N/A with this reason. Restored active filters remain independently removable and Clear all remains available. Component tests cover staged date/content controls without claiming production execution. Do not enable S05N folder/type/chat/sender/full-account filters from prototype fixtures.
9. [ ] **FIND-009 — Custom date range** — Pick From/To dates, cancel once, then apply a valid range; also attempt an invalid reversed range. → **Expected:** Cancel preserves prior filters, a valid range filters correctly, and an invalid range cannot be applied silently. This is currently a staged component-only contract while live native Date filtering is unavailable; do not claim native date filtering from picker tests. Existing From/To/review, zone snapshot, Cancel and reversed-range validation remain preserved.
10. [ ] **FIND-010 — Voice search** — Tap voice input, grant/cancel the system recognizer, and speak a synthetic query. → **Expected:** Success inserts recognized text once; cancellation leaves the query unchanged; missing recognizer shows a recoverable result.
11. [ ] **NCH-001 — New direct chat** — Tap New message and search for Bob using a synthetic profile already discovered on this installation. Observe cached matches while discovery continues, then a refreshed profile and relationship; repeat for a shared-group member who is not followed. Paste a valid npub whose profile relay is deliberately delayed, immediately tap its fallback row, and repeat while KeyPackage relay delivery is delayed. Select Bob and start a conversation. → **Expected:** Cached matches appear before search completes. A locally decoded npub row is actionable immediately without waiting for profile metadata; profile enrichment, KeyPackage prewarm and the authoritative existing-DM lookup overlap in the background. A failed prewarm remains recoverable through create-time validation, while an uncertain DM lookup fails closed instead of creating a duplicate. Updated names, profiles, ordering, and Followed badges replace the same account row without duplicates. Only direct follows receive the badge; sharing a group alone does not. Switching account/query discards old search updates. Existing DM is reused when present; otherwise the profile/start-chat flow creates one correct DM. Search uses the grouped New Message hierarchy: People when blank, then People in chats, Following and Network results only from actual native provenance. New group, Connect with QR and Invite a friend remain available during a query. Connect with QR opens Share & Connect with the current profile’s large QR and Scan QR action; there is no separate Show my QR row. Back from either level restores the original query. Verify native tap/long-press, followed announcements, light/dark/AMOLED, 200% text and RTL. Failed/partial discovery keeps local matches; Retry restarts the existing native subscription or unresolved address lookup without fake success. Clipboard/IME paste keeps its validated recipient boundary. A busy create disables competing actions and query edits. Chat created appears only with a retained canonical group ID; Open chat retries that ID without creating or republishing, and safe diagnostic Copy remains. Closing, switching accounts, signing out, wiping or replacing runtime cannot let old profile/prewarm/lookup/create/read results navigate or mutate the next account. A copied old row/action callback must also fail in the same frame, before recomposition. Close and reopen the scanner; previous session callbacks must not dismiss it or replace the query. Back/New group invalidates the old person intent immediately. The single New Message FAB retains New Message with New group, Connect with QR, Invite and paste capabilities. Known missing native receiving-relay configuration changes the icon into Check relays; only tapping opens native Relays and Back returns to Chats. Loading/unavailable/error never invent a missing-relay result. An old-account relay read cannot alter the current FAB. Native edit/Restore failures remain visible above the full-screen Relays child with Copy only for an explicitly copyable diagnostic report; consuming the activity snackbar must not hide the child notice. Check the AMOLED outline and expanded-pane alignment.
12. [ ] **NCH-002 — New group** — From New message select multiple contacts, continue, set a name/image or emoji, go Back/forward, and create. → **Expected:** Selections survive navigation, validation blocks an invalid setup, and one group containing exactly the chosen members opens. New group uses compact search, selected-person chips, segmented native rows and a pinned Continue/Create without other members action. Keep real local/discovered/followed metadata, identifier/self exclusion, partial/failure status and Retry; tap toggles, long-press previews the native person, chip removes and Review selected people remains reachable. No identifier scan auto-selects a member. Close/reopen scanner and reject old callbacks. Set Up Group has a 120dp avatar, Add/Change Photo menu (Photos, Files, web search, emoji, Remove), name with existing emoji action, description, disappearing messages and members. Name/description are submitted in the same native encrypted create; encrypted image bytes keep sourceUrl null. Verify photo processing/error, cancel/Back, real provider results, and name/description/timer/photo retained across setup Back; restored unsubmitted photos require explicit reselection/removal rather than silently disappearing. Verify light/dark/AMOLED, RTL and 200% short-window scrolling with reachable primary/emoji targets. Selection/draft/results and already captured callbacks must reject account switch, sign-out, wipe, runtime replacement and permanent departure before recomposition. Native configured-relay, missing KeyPackage, publication/encryption and projection failures remain authoritative; no mock relay count/readiness or local success is displayed. A canonical ID locks details and membership; Open chat retries that same native group without another create or retention update. Dismissing canonical recovery exits this group flow through its existing host callback, rather than reopening member edits. Check setup submission callback reentry/owner change before native work starts and no stale late image/read result mutates the replacement screen. Existing Add Members uses its unchanged picker contract.

### Conversation, composer, and delivery states

1. [ ] **CON-001 — Conversation header** — Open a DM, a three-member group, and a group chat containing exactly two members. → **Expected:** Header title/avatar/member context, back action, and details affordance identify the correct conversation. The two-member chat remains a group in its header/details while using compact message chrome; adding and removing a third member updates sender gutters without changing the route. Scroll deep into history and drag downward without reaching the start: the header stays compact with a 28 dp avatar; at the start it restores 40 dp. Tap the full title target to open the same chat’s details. Repeat restoration, RTL and 200% text; the title matches MarmotKit's prepared conversation title and the composer is disabled whenever the engine reports the account cannot send here. Give the DM peer a private nickname: the header shows it immediately, clearing it restores the prepared network name, a group keeps its own prepared title, and the other signed-in account never sees that nickname. A shared or pasted npub renders as a card showing that person's display name and picture when known, with the truncated npub still on the card, and a neutral glyph when nothing resolves. While search is open, every occurrence of the term is marked in the message text on any bubble colour.
2. [ ] **CON-002 — Transcript chronology** — Inspect a long transcript with date boundaries and multiple senders. Also open a conversation holding more than 50 unread messages, more than the 50 rows MarmotKit loads first. Repeat on a settled page without pending sends, then reopen a page containing both an agent stream start and its completed response. → **Expected:** Messages are chronological, date/unread separators are intelligible, consecutive-message grouping is correct, and latest content is reachable. Check inline date labels, the pinned date pill, full-width unread line and actual unread count in light/dark/AMOLED. Adjacent dates and membership events retain consistent spacing without changing message order or scroll position. The first unread message keeps one interval below the unread line rather than the divider's inset plus a sender-cluster gap. A conversation with unread messages opens at its first unread message rather than the newest one, even when that message lies beyond the first 50 loaded rows; the unread line's count equals the real number of unread messages, which MarmotKit does not cap; and opening alone does not mark anything read. Open the keyboard from the composer and dismiss it with Back: the transcript stays glued to the keyboard for the whole animation in both directions, with no jump when it settles, and the newest message stays against the composer throughout. In a bubble whose body runs to more than one block, a paragraph followed by a link, the timestamp and delivery glyph sit on the last line rather than mid-bubble. A message still sending shows a pending clock rather than a spinner, at the same size as the delivery tick beside it. Native row order remains unchanged on the settled page; a completed response never restarts a live stream or creates a duplicate bubble. Scroll so an unread message passes beneath the composer without clearing it: that message stays unread and keeps its place in the count until it rises clear of the composer, and the same holds for the row behind the conversation's title bar. Rows reach the composer at full strength and then thin away behind it to nothing by the bottom of the screen, and dissolve into the bar above rather than being sliced off mid-bubble; confirm the fade in light, dark and AMOLED, where a covering scrim would show as a grey band.
3. [ ] **CON-003 — Pagination** — Scroll repeatedly to the oldest prepared messages, briskly enough to cross several pages without pausing, in a conversation holding more than 200 messages. Then repeat while the engine is busy, for example on the first launch after a long time offline, and with **Settings → Developer → Diagnostics → Performance logs** enabled. Create a reply whose visible quote contains an original outside the current retained window, then tap that quote while the engine is busy and again after it settles. → **Expected:** Older pages load early enough that a fast fling crosses ordinary page boundaries without stopping and restarting; pages contain no duplicates, jumps, or lost content; end-of-history is stable; the bottom-anchored transcript extends its far end without moving the row being read; older and newer pages extend the window without losing the row you were reading. A hydrated reply target outside the loaded rows recenters and highlights its exact original; a delayed window command asks the reader to retry instead of claiming the already displayed original is unavailable, while only an explicit native missing-target result shows the unavailable message. After enough older paging to evict the last-read message from the bounded window, the jump badge still shows the authoritative unread count rather than counting the loaded history; tapping it returns to the physical newest edge in one action. The row under your finger stays put across every page, and rows already on screen do not visibly re-render or re-lay-out after a page arrives. When a page cannot be answered before the engine's deadline the "couldn't load" retry row appears once and paging stops until you tap it, instead of a spinner that sits and then silently gives up; tapping it pages again. Window preparation does not stall scrolling or composer input, and if two updates overlap only the newest prepared generation commits. With performance logs on, `WNPerf` lines for `op=chat_history_page` report `page_window` and `page_apply` for each page, and `page_apply` stays well below `page_window`.
4. [ ] **CON-004 — Incoming foreground message** — Keep Alice's chat with Bob open while Bob sends. Repeat with many rows and replies from the same few senders. → **Expected:** The message appears once, appropriate haptic/visual feedback occurs, and no redundant notification remains for the foreground chat. With the transcript at its newest edge, the arriving bubble rises from beneath the transcript while the rows above slide up by its height in one short motion, the same entrance a sent message gets. Repeated senders and reply authors retain the correct names and avatars as the page updates.
5. [ ] **CON-005 — Plain send** — Type a unique short message and tap Send. In a group, insert an `@` mention, move the caret into its canonical token with an IME composition still active, continue typing, and send. → **Expected:** The composer clears once, a pending/sending state becomes sent, Bob receives exactly one matching message, and ordering is correct. A composing range that falls inside a shortened mention label temporarily shows the canonical token instead of crashing; the friendly label returns when the IME commits, and the sent mention remains canonical. Send a message that filled the composer with two or more lines while at the newest edge of a chat with history: the composer collapses to one line in the frame the text leaves, and in that same moment the new bubble rises from beneath the transcript while the rows above slide up by its height, one short motion of about 180 ms with nothing popping in first or sliding afterward; only a reader who sent from up in history is carried down to it instead. Repeat after paging the bounded transcript back to an older date: the window returns to latest before revealing the optimistic row, so today's send appears at the physical conversation tail rather than beside the old date. The settled delivery disc reads as quieter metadata than the timestamp beside it, while the pending ring and a failure stay prominent. The sent bubble is drawn once throughout: also when Bob's relay echo reaches Alice before her send call returns, and also in a group with a disappearing-message timer or a mention, where the echo carries tags the typed message did not. Send repeatedly on a weak connection: an accepted send never raises a paging error of its own, the transcript and the sent bubble keep their state, and the app does not sit re-fetching. Scrolling deliberately towards unavailable newer messages still offers the bottom-edge retry, which clears once newer content arrives.
6. [ ] **CON-006 — Send failure/retry** — Disable networking, send a unique message, restore networking, and use Retry. → **Expected:** Failure remains visibly attached to the unsent message; retry sends that message once and removes the failure state.
7. [ ] **CON-007 — Multi-line/Enter behavior** — Test both Appearance → Enter key settings using the same draft. → **Expected:** Send mode sends on Enter and New line mode inserts a newline; the visible send button remains usable in both. Pasting or dictating several lines at once grows the composer in one motion with no step back, and a draft long enough to scroll keeps its text, caret and selection handles clear of the scroll thumb beside it.
8. [ ] **CON-008 — Draft lifecycle** — Type multiline text, switch chats/accounts, background, and relaunch before returning. In a second run, keep typing immediately after tapping Send, and in a third run edit the same draft on two devices signed into the account. In a fourth run, send a message, wait for it to read as sent, then force-stop the app within a second of that and reopen the chat. → **Expected:** Draft belongs only to its original account/chat and restores exactly; clearing it remains cleared; text typed after Send survives the sent message's acceptance instead of being cleared with it, and a draft edited elsewhere is reloaded rather than overwritten by the stale editor. After the force stop the composer is empty: a keystroke save still queued when the send completed must not reappear as a draft, and the sent message stays in the transcript exactly once rather than returning as text to send again.
9. [ ] **CON-009 — Empty and oversized input** — Try spaces-only input and a message at/over the supported limit. → **Expected:** Empty content cannot send; length behavior is explicit and never truncates or duplicates silently.
10. [ ] **CON-010 — Emoji picker** — Open emoji, browse/search categories if shown, insert several emoji, then dismiss by transcript tap and Back. → **Expected:** Emoji insert at the cursor; dismissal does not erase the draft or leave the keyboard/pane stuck.
11. [ ] **CON-011 — Mentions** — In a group type `@`, filter/select Bob, send, and have Bob open it. → **Expected:** Picker shows eligible members, the mention renders distinctly and links to Bob's profile, and Bob gets mention behavior rather than plain-text behavior. In AMOLED, a received self-mention uses one complete highlighted bubble outline without a second interior rail; the highlight remains distinct when that side has a custom outline color, including plain and media/caption bubbles at 200% text and RTL.
12. [ ] **CON-012 — Reply composer** — Reply to an older text and an attachment, edit the reply draft, cancel once, then send. → **Expected:** Reply context identifies the source, cancel returns to normal compose without sending, and sent reply navigates back to the source when tapped. The quote shows a rounded inset rail and up to two excerpt lines. Its small corner X has a48dp touch target; canceling retains the draft and never sends. Repeat warning, unavailable source, typed media, RTL and 200% text. In AMOLED, inspect compact and multiline reply states and confirm the preview owns one continuous rounded outline with all four corners intact and no border/content bleed.
13. [ ] **CON-013 — Composer/IME transitions** — Alternate keyboard, emoji, attachment pane, and transcript taps; rotate once. → **Expected:** Only one input surface owns the space, the composer stays above system insets, and focus/draft remain correct. Check the shared rounded composer surface and action order: Add/emoji lead, sending and speech controls trail. Draft/focus moves text above the action row; compact reading height is48dp. Send stays one native acceptance operation.
14. [ ] **CON-014 — Scroll/new-message control** — Scroll far from latest, receive several messages, then use the jump-to-latest control. → **Expected:** Reading position is not stolen; unread/new count is honest; the control reaches the latest message; after jumping to the newest message the transcript follows new arrivals again.
15. [ ] **CON-015 — Conversation load failure** — Open a cached chat while offline and trigger older-message loading. Also open chats on an account MarmotKit is still finishing after an upgrade (INT-010). → **Expected:** Cached transcript remains readable, failure is placed without covering controls, and retry works after reconnection. If MarmotKit refuses the conversation window, the chat still opens through the plain timeline subscription (prepared title and jump-to-message are unavailable until the window returns); a not-ready or timed-out window shows the calmer "still finishing" / "taking longer than usual" copy instead of "something went wrong", and the report names the engine error (`marmot=…`).
16. [ ] **CON-016 — Timeline warnings and invalidated content** — Open fixtures containing deleted, invalidated, partially visible, and noncanonical-history messages, then retry older-history loading. → **Expected:** Each condition has distinct explanatory UI, retained valid messages remain usable, and Retry never converts unverified content into an ordinary message. A message an admin removed says so, rather than reading as an anonymous deletion, including when this account authored it; a tombstone the engine cannot attribute keeps the passive wording.
17. [ ] **CON-017 — Retained composer height** — Enter a multiline draft, drag the composer shorter and taller, toggle full screen, tap the composer’s top border or use its accessibility action to return to Automatic, then select a manual or full-screen height, dismiss the keyboard, return to Chats, and reopen the same conversation; repeat after switching chats and accounts, then send. Also start dictation in a manually expanded/full-screen composer, leave and return, then Send from the persistent controls; repeat with a failed send and with a newer draft or resize made while send is pending. → **Expected:** The manual or full-screen choice survives focus, IME dismissal, Back, re-entry, and restored navigation only for its owning account/chat; Back adds no collapse-only step, the border/accessibility action can explicitly return to Automatic, live bounds remain safe, and the override clears only after durable send or removal of its owning group or account. The composer's top border shows a visible grip whenever it is tall enough to resize, and the whole border stays draggable. Dictation controls do not overlap the resize affordance, successful dictation Send clears only the captured origin draft and height, and failed/stale sends or newer draft/resize choices retain their geometry. Reply/edit previews sit inside the same rounded surface; their cancel targets remain independent of the invisible top-border resize region. A resize released between the endpoints keeps exactly the height it was let go at, and that height survives the same way a full-screen choice does; releasing within 24 dp of the automatic or full-screen endpoint still lands on that endpoint. Canceled drag retains the prior stored choice. Legacy saved manual heights still restore safely.

18. [ ] **CON-018 — Recent message times** — Open prepared text, reply, image/album, file, pending-media, and focused-preview messages whose timestamps are under one minute, 1 minute, 59 minutes, and exactly 60 minutes old. Keep the conversation resumed across a minute boundary, then background it across another boundary and resume. Repeat with 12-hour and 24-hour system time. → **Expected:** Every bubble-footer surface uses the same localized contract: `now` for 0–59 seconds, compact plural-correct minutes from 1–59 minutes, and the localized clock from exactly 60 minutes onward. Resumed labels advance on the minute boundary; backgrounded labels do not run a visible refresh loop and update immediately on resume. Expanded message details and conversation date separators keep their absolute date/time roles, and delivery, edited, and retention metadata remain aligned.

19. [ ] **CON-019 — Wave after a group invite** — Have an admin add Bob to a group, then tap **Wave hi** beside Bob’s member-added row. Repeat with an unsent draft, an older invite, a long member name, each supported app language, dark/AMOLED, RTL and 200% text. Go offline, wave, reconnect and retry the failed message. Reopen the chat, restart the app, switch accounts, and have Bob leave and join again. Also attempt a wave when sending cannot start. → **Expected:** One tap sends `👋` followed by a real mention of Bob, reveals the outgoing message and preserves the draft. Bob receives one greeting with mention behavior. The action is absent for your own addition, unrelated or unauthenticated system rows, and when sending is unavailable or you are editing/replying. Long names and large text wrap without hiding the action; TalkBack identifies Wave hi as a button. The button disappears for that invite once the send is accepted and stays hidden after a later send failure, reopening the chat, or restarting the app; use the failed bubble’s Retry. A send that cannot start leaves the button available. Dismissal belongs only to that account, group, and invite event; another account or a later invite for Bob still offers Wave hi.

### Attachments, media, contacts, and location

1. [ ] **MED-001 — Attachment pane** — Tap Attach in a conversation. → **Expected:** The anchored menu offers Camera, Photos and videos, Files, Location, User and Contact with no Recent media row; opening the menu preserves the keyboard. Choose Photos and videos to open the system photo picker directly, without an intermediate sheet or broad media permission request. Verify cancellation returns safely, menu dismissal, and large-text/RTL reachability. The anchored menu sizes to its widest label rather than the conversation's width, in every locale and at large text.
2. [ ] **MED-003 — Gallery image** — Select a small image, cancel preview once, then select and send. → **Expected:** Cancel sends nothing; preview is accurate; sent image renders on both devices with delivery state. Selection stages thumbnails inside the composer; tap a thumbnail to preview/edit, then close back to the draft without sending. Send once with an empty caption and once with a caption. During preparation, preview/removal and duplicate Send are disabled; rejection restores the staged draft, and acceptance clears only the captured items and unchanged caption. Append another item while sending: it remains staged afterward. Open a staged item: the optional full preview uses the current light/dark/AMOLED theme and preserves native Edit, quality, Add and Remove actions. Closing returns to the same composer draft. Send the image with a caption: on both devices the caption sits inside the same bubble under the image, and the time and delivery tick sit on the caption's last line rather than above it; the caption stays after the pending clock becomes a tick, and it survives closing and reopening the chat. A substantial caption widens a narrow portrait-image bubble enough to read comfortably without stretching the image; incoming media stays start-aligned and outgoing media stays end-aligned, while a short caption keeps the media-led width.
3. [ ] **MED-004 — Camera permission and photo** — Tap Camera, deny permission once, then grant and capture a photo. → **Expected:** Denial returns safely with guidance; grant launches capture; cancel sends nothing; accepted photo reaches preview/send.
4. [ ] **MED-005 — Photo quality** — For a large image open the quality chooser and send with each offered quality in separate messages. → **Expected:** Selection is visible, each upload completes, dimensions/size reflect the choice, and the preview is not rotated or corrupted. The copy says photo location and device details are removed even at Original, that videos and imported audio keep their original data, and that bitrate applies only to recorded voice notes.
5. [ ] **MED-006 — Photo editor** — Open an image in the editor, apply every available transform/markup, undo/cancel once, then save/send. → **Expected:** Cancel preserves the original and sends nothing; saved edits match preview and remain readable after download. The quality explanation says photo location and device details are removed, Original preserves supported image data, other formats are re-encoded, and videos and files are unchanged. Check themed Photo editor/Close/Save header, labeled Undo/Redo/Reset/Rotate actions and labeled crop/draw/erase chips. With the crop tool active, drag each corner to resize and drag inside the rectangle to move it: a drag that misses a corner moves the crop rather than doing nothing, a drag past an edge slides along it at the same size, and each completed resize or move undoes on its own. Rotate after moving the crop, save, and confirm the sent image is the region that was selected. In wide/short windows the side panel keeps positive photo width, including 240dp width; scroll each tool and stroke-width choice into view.
6. [ ] **MED-007 — Video** — Select and send a supported video, then play/pause/seek/full-screen on both devices. Rotate portrait → landscape → portrait while playing, repeat while paused after seeking, and rotate once while a slow download is pending. Close the viewer, reopen it, and switch accounts with it open. → **Expected:** Upload/download progress is honest, thumbnail and duration are sensible, and controls work. Rotation keeps the selected video, playback position and playing/paused intent without restarting playback or duplicating audio; a pending download completes in the same viewer. Closing the viewer or switching accounts stops its playback; no prior account's video remains visible. Watch the whole send, from the composer preview through upload to the confirmed message: the video keeps a recognizable poster the entire time, with the sending, failed and retrying state drawn over it, and never turns into a file row listing its name and dimensions. Repeat with the network off so the upload fails and is retried, with a video-only album, with a mixed photo and video album, and with a document sent alongside them — the album keeps its tile count, order and corner treatment across the swap, and only the document is a file row. Send a deliberately damaged video file too: it falls back to a video row with its name, never to a document row.
7. [ ] **MED-008 — Audio/voice message** — Record or select audio, grant/deny microphone where applicable, send, then download/retry/play/pause/seek. → **Expected:** Denial and failed downloads are recoverable; downloading and recording state are clear; the message is delivered once and playback state is understandable without moving the reading position. Releasing a recording or pressing locked Stop opens voice review without an outgoing message. Replay/pause the actual take, discard, re-record, then explicitly Send. Rapid Send queues once; preparation failure retains the take for Retry. Discard or leave/switch account while preparation is pending: the old take cannot queue. After discard, typed text, selection, reply/edit and staged attachments return. Verify the short-height and 200% RTL review controls. Recreate the Activity with a finalized unsent take: if audio is unavailable, Recording unavailable offers Record again or Dismiss, never Send or fabricated playback. A different account/chat must not inherit this notice or its callbacks.
8. [ ] **MED-009 — Document** — Pick a PDF and another supported document, cancel once, send one, exercise an upload failure and Retry, then send an image plus document and open/download the files. Before one send, stage an image, video, and document with an empty caption; return to Chats, reopen the conversation, then force-stop/relaunch and reopen it again. → **Expected:** Filename/type/size and progress are correct; cancel sends nothing; the temporary pending/failed row becomes one confirmed row without stale Retry; the last document card owns one timestamp/delivery footer, including in the mixed message; and Android opens each downloaded file with a compatible app. The attachment-only draft restores every staged item from native bytes after navigation and relaunch, even if the original picker grant is no longer readable; remove a restored document immediately while the draft is still loading and confirm it never reappears; removing an item deletes only that native draft attachment, while merely leaving the screen never does. Stage the same document twice and append another during sending: acceptance removes only the captured occurrences. Removing or previewing staged items stays disabled while preparation is pending, and rejection retains all items. Send the PDF with a caption as well: the caption renders under the file card on both devices with the time and tick on the caption's last line, and it stays once delivered.
9. [ ] **MED-010 — Text attachment reader** — Open a sent plain-text file, search/select/copy or rename where offered, rotate, then close. → **Expected:** Content and filename render safely, state survives rotation, copy uses selected text, and Back returns to the chat. Check File header, external-open overflow, scrollable full filename/MIME and bottom Copy text/Read aloud buttons. Select text and press Back: first Back clears selection, second leaves the reader. At 200% RTL/short height, reach all bottom controls by scrolling. Verify actual loaded byte size, Save from overflow, and selected-text Read aloud. Stop reading stops only this attachment. Cancel a save destination, retry, then leave/switch account during preparation: the old reader cannot publish a save or start speech.
10. [ ] **MED-011 — Unsupported/oversized file** — Select an unsupported and an over-limit fixture. → **Expected:** The app rejects it with an actionable reason before or during send and never posts a misleading successful bubble.
11. [ ] **MED-012 — Location share** — Open Location, grant coarse or selected permission, choose a synthetic place, cancel once, then share. → **Expected:** Permission disclosure matches the request, cancel sends nothing, and the sent card opens the intended map coordinates without leaking a prior location.
12. [ ] **MED-013 — User share** — Choose Bob from User and send the profile card; tap it on Carol's device. → **Expected:** The card identifies Bob by current profile/`npub`, opens Bob's profile, and never starts a chat as the wrong account.
13. [ ] **MED-014 — Contact share** — Tap Contact, confirm White Noise does not request contacts permission, select a synthetic contact in Android's contact picker, inspect the preview, cancel once, then repeat and send. → **Expected:** Cancel sends nothing; only the selected contact's displayed name and phone number are previewed/shared; the card is clearly a phone contact rather than a White Noise identity.
14. [ ] **MED-015 — Full-screen image viewer** — Open a three-message image transcript whose middle message is a two-image album, swipe pages, zoom/pan, rotate, close, and reopen the same cached image. Repeat the viewer's Close, Save and Share controls in landscape with large text and a display cutout where available, then repeat Back from a video opened while reading older messages. → **Expected:** The shared-media grid remains newest-first while the viewer traverses oldest-to-newest, each album keeps its authored slot order, and the later image is spatially to the right in both LTR and RTL. Reopening uses the safe cached thumbnail or thumbhash as its first frame instead of flashing a blank spinner, then refines only the current page without prefetching neighbours. Correct media/page indicator persists, gestures do not trigger chat navigation, controls and sender/time remain readable and reachable outside system bars and cutouts, and Back restores the originating transcript position without a jump to newer messages. Check sender, time, current-page metadata, Close, More → Save and bottom Share in light/dark/AMOLED and 200% RTL. A single image tap hides both chrome bars for an unobstructed view and another tap restores them; a double tap resets zoom without also toggling chrome. Hidden chrome remains hidden while paging between images, while reaching a video restores its controls. Swipe to another page while More is open: the old menu closes and every action targets the current page. Forward message opens the native picker for the complete current source message, including every original album attachment and caption. Go to message jumps to the exact viewed source. Old page/epoch/account callbacks cannot forward; reopening after recreation restores only the correct native encrypted pending request.
15. [ ] **MED-016 — Download/save/share** — From supported media use Save and Android Share. → **Expected:** Save creates one readable file with correct content; Share opens a chooser with only intended content/URI access and returns safely. Share a message with one attachment and one with several to a receiving app that shows filenames: each file arrives under its own original name (`story.pdf` stays `story.pdf`, never `message_<random>_story.pdf`), two attachments with the same name both keep it, and a name with path segments or control characters arrives sanitized.
16. [ ] **MED-017 — Missing/expired/restricted media** — Open prepared messages representing pending, failed, malformed, expired, unavailable, and restricted attachments, plus one prepared message whose attachment MarmotKit rejected as malformed or unsupported → **Expected:** Each state has distinct honest copy and only valid recovery actions; forwarding is disabled with the corresponding reason; a rejected attachment shows the Attachment unavailable placeholder in its slot while the message text and any accepted siblings still render, and Save/Share cover only the accepted attachments.

   Automated companion coverage for `MED-017`: `AttachmentDownloadWorkerClassTest` verifies immediate terminal failure for completed-but-unretained bodies, bounded transport retries, cancellation, and explicit subsequent requests. Release testers do not need to induce cache publication failures or inspect WorkManager internals.

17. [ ] **MED-018 — APK attachment installation safety** — Open controlled valid, tampered, unsupported-ABI, and non-APK files named as APKs; test without an installer and with install-unknown-apps permission denied, then grant only for the controlled valid file. → **Expected:** Only a verified compatible APK reaches Android's installer; invalid/unsupported/no-installer/permission states are explicit, and no attachment is silently installed.
18. [ ] **MED-019 — Host-managed automatic attachment acquisition** — Record native retained-byte, disk-reserve, and transfer-limit values; install the 0.10.4 candidate in place on a disposable emulator, reopen retained chats, and create/import an identity. Keep uncached attachments off screen, then expose eligible photo/video/audio/file cards. Toggle each type, pause automatic downloads, switch Wi-Fi to metered/roaming/mobile, disconnect, and resume. Repeat these changes while an attachment is downloading; scroll, type, and navigate Back during the transfer. → **Expected:** Runtime construction starts with host permission denied and never backfills off-screen history. The former containment flag is enabled only under HostManaged; numeric limits and explicit pause/cancel intent survive. Each account/type follows its current restrictive network matrix. The app remains responsive throughout permission changes, with no freeze or Android not-responding dialog. Stale callbacks cannot restore Wi-Fi permission. Android requests native automatic demand, never legacy HTTP or explicit retry. Quota/reserve pressure remains native-owned; terminal exhausted/unretained/unavailable acquisitions require deliberate recovery, not reopen/recomposition retries.
19. [ ] **MED-020 — Native and legacy retained attachment reads** — On a disposable emulator, prepare native-only, legacy-cache-only, both-store, and absent fixtures; include zero-byte, multi-chunk, cancelled, invalidated, and source-edited attachments with differing display/source IDs. Open, replay, share, save and forward; rotate/leave during a transfer, explicitly cancel another while the device is busy, restart, and install the candidate in place. Record transport bytes before/after each operation. → **Expected:** Eligible native or authenticated legacy local bytes are read before demand, with zero attachment network bytes for local hits. Original source/album indices and existing WorkManager cancellation identity survive upgrades. Native leases remain bounded and close on failure/cancellation, including cancellation on the final read chunk or a later item failing in a multi-asset batch. Leaving a screen closes observation only; Cancel separately persists native cancellation, with the status query and control command completing as one native IO turn even under load. Both actions remain responsive while transfer updates are arriving. Missing automatic attachments use idempotent native demand and native retry budgets. No automatic cache-miss fallback to downloadMedia, no forced migration redownload, no new persistent Android copy of native-owned bytes. No downgrade or destructive data reset is used. Legacy bytes already evicted before the upgrade cannot be reconstructed from native acquisition history.
20. [ ] **MED-021 — iOS GIPHY envelope rendering** — From iOS, send GIPHY messages with and without a creator attribution, then open the conversation on Android under allowed, disabled, paused, offline, slow, redirect, wrong-MIME, and oversize media conditions. Exercise manual load/retry, scroll the card off screen and back, chat-list/reply/notification previews, and one malformed or lookalike-host envelope. → **Expected:** Exact approved envelopes render as bounded inline animated cards with visible GIPHY attribution and no Android-only URL action buttons; policy-disabled or paused media waits for a deliberate load and active work stops when disposed. Redirects stay on approved HTTPS GIPHY CDN hosts and invalid MIME/oversize/malformed inputs fail closed without exposing the raw CDN URL in compact previews. Failures can retry, and ordinary text or lookalike URLs continue through normal Markdown rendering.

### MarmotKit durable sends and preview retention

1. [ ] **MSG-017 — Durable caller-correlated send admission** — Send text, a reply, and staged media while publication is delayed; navigate away, interrupt the caller wait, restart, and submit a second message with identical text. With controlled runtime fault injection, close the transport once before MDK records ownership and once after it records ownership. Edit the composer after pressing Send. → **Expected:** The bubble appears immediately, while local acceptance remains Sending rather than claiming relay delivery; the checkmark appears only after the published timeline projection. A pre-ownership closure retries the same client token, while recorded ownership returns pending without another admission. Native timeline tokens reconcile only their original optimistic bubble. Retained ownership is queried before re-admission or re-upload; rejected identity collisions do not silently create another send. A revision-bound media acceptance consumes only its selected draft, preserving newer edits. Non-draft media fallback remains on its existing completion contract and must not be described as durable token adoption. Verify unknown outcomes, offline convergence, epoch-invalid media, and deliberate retry separately. With an external signer set to manual approval, share an image into a chat and reject the first signing request: the Send failed notice stays until dismissed. Retry the failed bubble and approve it — the notice goes on its own, without restarting the app — while a second rejected retry keeps current failure feedback, and a failure in another chat or profile is never cleared by someone else's recovery.

2. [ ] **MSG-018 — Pinned chat-preview expiry** — Keep the chat list visible through a selected message's expiry, background across another deadline, and resume. Change the group's duration between messages; repeat with a selected draft/invitation, unknown duration, zero duration, and missing expiry. → **Expected:** Only an expired selected-message preview becomes empty, including its attachment icon; no new list event is required. Drafts/invitations remain visible, unknown/disabled/overflow cases are retained, and native row ordering/unread state does not change.
3. [ ] **MSG-019 — Cancel a pending send before native admission** — Delay Markdown preparation for a new text and reply send, then use the pending bubble's delete action before MarmotKit admission begins. Repeat with an image, video, document, and voice-note upload delayed before admission, and with a failed send immediately retried. Instrument or log normal message-deletion calls during each cancellation attempt. In separate runs let native admission win first, returning both accepted-pending and published outcomes, then invoke a stale captured delete action. → **Expected:** A cancellation that wins removes the optimistic bubble and chat-list preview, releases retained attachment bytes, and never publishes the text, reply, or media event; it makes zero normal message-deletion calls with the temporary optimistic ID, and repeating the same cancellation is harmless. If native admission wins, the send stays pending or settles normally and the stale delete cannot claim cancellation or remove it. Unknown acknowledgements remain pending for authoritative reconciliation. Retry and cancellation serialize per conversation without resurrecting a removed bubble or duplicating a send.

4. [ ] **MSG-020 — Amber grouped-send rate-limit boundary** — On the GrapheneOS Android 17 fixture with Amber 6.6.4 or newer, use a disposable Amber account first with remembered grants and then with foreground approval required. Send a single message, then send enough rapid consecutive messages to cross Amber's default five-new-screens-per-30-seconds boundary. During a controlled foreground run, hold the first signer activity before it is ready and admit at least six same-account requests; separately switch accounts, rotate/recreate White Noise, background/foreground it, deny one addressed request, cancel one visible group, allow one request to time out, and deliver a late result. → **Expected:** ContentResolver grants remain prompt-free. Foreground work opens one cold approval screen and merges the burst into that account's bounded Amber session; a sixth sequential screen waits for the local budget instead of producing Amber's “Too many requests” toast. Every request completes exactly once, mixed approvals stay correlated by ID, and another account or late result cannot complete or cancel the active session. Explicit denial remains a signer rejection; launcher loss or expiry of the local screen budget is retryable/unavailable, while an ambiguous post-launch timeout is not automatically resubmitted. There is no duplicate or lost message. Privacy-safe diagnostics contain only operation type, grouped admission/merge state, launch count, and terminal category—never event JSON, plaintext, keys, account/group/message/request identifiers, signatures, packages, or signer results.

### Message rendering and actions

1. [ ] **MSG-001 — Text and links** — Send plain text, punctuation, emoji, multiline text, a URL, and selectable text. → **Expected:** Text is exact, links are visually distinct and open only after deliberate tap, and selection/copy preserves characters.
2. [ ] **MSG-002 — Markdown** — Send supported headings, emphasis, lists, quote, inline/fenced code, link syntax, and closed/open `<details>` blocks with a summary and spaced body paragraphs. → **Expected:** Supported Markdown renders consistently in bubbles and full-screen text; code remains legible and copyable. Details honor the initial open state, toggle on summary tap, and retain body formatting and spacing. Chat previews and read-aloud include summary/body text without structural tags; embedded event references remain discoverable. The full-screen reader uses a plain padded document canvas with author/time header. More retains the current Reply/React/Copy/Delete capabilities; Back clears native text selection before closing.
3. [ ] **RDR-001 — Nostr identifiers/events** — Send valid `npub`/`nprofile`, `note`/`nevent`/`naddr`/`nrelay`, and malformed Nostr identifiers, then tap each. → **Expected:** Resolved `npub`/`nprofile` values open the in-app profile; unresolved profiles fall back to shortened code-style text; event/address/relay identifiers stay styled but inert; malformed values remain harmless literal text.
4. [ ] **MSG-004 — Long-press menu** — Long-press incoming/outgoing text and each attachment type. → **Expected:** The anchored menu stays on-screen and offers only valid actions for ownership/type/state. The focused preview appears over the dimmed, blurred conversation with a reaction rail and labeled actions. Text preview is limited to five lines; preview links/replies/media cannot activate. Scroll a long menu at200% text and RTL; repeat above the keyboard with a tall portrait media preview and confirm the actions retain a visible, scrollable touch area. Inspect disabled forwarding reasons, use Back with the keyboard open, and switch accounts while open. Only the original message’s current valid actions can execute. Every part of the overlay that is not an action dismisses it — the scrim, the empty stack space and the lifted message itself, which a screen reader can also dismiss from — while actions and reactions still act. Back closes the overlay before it leaves the conversation, and leaving or switching chats clears it: the next chat opens with no overlay and cannot show or act on the message the previous one lifted. Keep on screen leads the actions so it always falls in the grid's first row, and Report closes them. The actions sit in a two-column labelled grid where both columns can hold their labels, with the last action keeping its column rather than stretching across the row; at large font or a narrow menu it becomes one readable column with every label shown in full. Drag the lifted message up and down and fling it: the stack follows the finger and settles against the top or bottom of the safe area without sliding under the status bar or the keyboard. With the keyboard open and every action available, long-press a low bubble: the lifted message and reactions remain visible while only the action grid scrolls, and the last action is reachable without closing the keyboard. Repeat at 200% text and in RTL.
5. [ ] **MSG-005 — Quick/custom reaction** — Add a quick reaction, open the full emoji picker for another (search, then jump with the category bar), tap each displayed reaction pill, use the All and emoji filters in the reactor list, and remove your own reaction from the row labelled Tap to remove; long-press a pill and tap a "+N" overflow pill to open the unfiltered list. While one emoji filter is selected, have another participant add or remove a different reaction, then remove the selected emoji entirely. Rapidly remove and re-add the same emoji on a delayed connection. From the picker open Configure Reactions, replace a slot, Reset, then Done. While offline or on a delayed connection, add and remove reactions on two different messages, then reconnect. → **Expected:** Every pill tap opens the reactor list on All and never publishes a reaction mutation or shows a reaction error. Tapping your labelled reactor row removes only that row's emoji; tapping another reactor opens their profile. Live participant changes retain a filter selected inside the sheet while that emoji exists and return to All only when it disappears. Counts/actors synchronize on both devices, duplicate self-reactions do not accumulate, details identify reactors, and the message action row shows the configured quick reactions. With all six slots configured, every one of them and the full-picker button are on screen the moment the popup opens on a 360 dp-wide device — the rail wraps onto a second row instead of parking a choice past a sideways scroll — and each keeps a 48 dp target in LTR, RTL and at the largest font. Every tap updates the local reaction state immediately; same-message, same-emoji choices conflate to the latest intent so rapid changes never overlap or show a generic reaction error. A reacted message adds only the height of the chips it draws to the transcript rhythm, so the next message — and the composer, for the last row — keeps its ordinary spacing. Each pending change adjusts only its own message, with no reaction count leaking between targets.
6. [ ] **MSG-006 — Reply navigation** — Tap a reply preview whose source is loaded, then one requiring older history. → **Expected:** The transcript reaches and identifies the exact source or gives a clear unavailable result without jumping elsewhere.
7. [ ] **MSG-007 — Edit message** — Edit Alice's sent text, cancel once, then save a real change and inspect edit history. First open a page of ordinary unedited messages, then perform the edit and reopen that page. Delay delivery of a new text message, edit its pending bubble, and submit the revision before confirmation; repeat while keeping the editor open until confirmation and while a separate unsent composer draft exists. Also submit revision A while the original is pending, reopen Edit on that bubble, let the original confirm, then cancel the reopened editor. Retry after a definite original-send failure and after a failed edit. → **Expected:** Only Alice's editable message offers Edit; cancel preserves it; update marks Edited and history shows correct versions/times. A pending revision remains visible while delivery settles, then targets the confirmed event rather than the local client token; cancelling the reopened editor publishes the earlier submitted revision A exactly once after confirmation; the separate draft and in-progress edit text remain intact through the handoff. A failed original remains retryable without a duplicate send, and a failed confirmed edit keeps its retry action. Edit history is a full-height view of the accepted revisions MarmotKit holds, newest first, numbered by the engine's edit count even when the loaded window never saw the older edits; because the bubble already shows the edited body, no row is labelled Original unless the engine could not answer and the window's own edits stand in. Timestamps are exact, localized and carry the timezone. Text remains selectable; malformed timestamps cannot crash the view or invent a time. Ordinary messages never acquire an Edited marker or invented history; accepted edits still show their exact revisions.
8. [ ] **MSG-008 — Delete message** — Delete a disposable outgoing message, cancel once, then confirm each offered local/everyone scope. → **Expected:** Dialog explains scope; cancel preserves; confirmed deletion updates only intended devices/content and leaves no false success. Also long-press a member-added system row and choose **Delete for me**: only that local row disappears, the peer keeps it, and no greeting is sent.
9. [ ] **MSG-009 — Copy/select/share** — Use Copy text, Select text, and Android Share on supported messages. → **Expected:** Clipboard/share payload equals the chosen content, sensitive extras are absent, and chooser cancellation does not mutate the chat.
10. [ ] **MSG-010 — Forward one/many** — Forward a supported message to one chat and then multiple chats using search and direct selection. Next select an outside chat, use a folder chip with at least two eligible destinations, deselect one member directly, and tap the chip through mixed, On, and Off states before confirming. → **Expected:** Origin and ineligible chats are excluded. Off or mixed selects every eligible folder member; On removes only that folder's members and preserves the outside choice. The chip's count and checkbox state match the destination rows, progress names each target, every target receives one freshly sent payload, and no source encryption or author attribution leaks.
11. [ ] **MSG-011 — Partial forward failure** — Make one selected target unavailable and forward to it plus a valid target. → **Expected:** Per-target progress/result distinguishes success and failure; Retry affects only failed targets and does not duplicate successes.
12. [ ] **MSG-012 — Multi-message selection** — Enter Select, choose mixed incoming/outgoing messages, scroll, deselect, and apply each enabled batch action. → **Expected:** Selection/count survives scrolling, invalid destructive actions are disabled, and only selected messages are affected. The toolbar says Select messages while accessibility announces the actual selected count; Close exits the same selection without dispatching an action.
13. [ ] **MSG-013 — Message info** — Open Info for sent, received, pending, failed, and attachment messages; compare each document card's visible footer with Info. → **Expected:** IDs/timestamps/sender/delivery details match the message; an outgoing document shows its applicable delivery state while a received document shows its time without a sent-state icon; values can be copied where offered; and no unrelated secret is shown.
14. [ ] **TTS-004 — Natural structured speech** — Speak “Yes. No! OK. Done.”, “Pay $12.50. Ship 2.5 kg.”, a fenced code block, and an arrow diagram; repeat with an unsupported voice language. → **Expected:** Every short sentence is read, supported English amounts and units expand without changing display/copy text, code keeps operators and offers Read code literally in the message menu, diagrams narrate only proven relationships or fall back line by line, and unsupported languages retain literal text. Select text in the second sentence of a Markdown message (including a bold amount), then choose Speak from the selection menu; playback must start at that sentence without repeating the first. Double-tap each repeated amount and a quantity split across formatting. The intended sentence restarts and every spoken part of an expansion highlights its complete original amount; stale callbacks after seeking never move the cursor. With TalkBack, use Read from this sentence on a one-sentence leaf and Choose sentence to read on a multi-sentence leaf; selecting the second duplicate excerpt reads sentence two. An edit closes the chooser. During initial Preparing speech, Stop remains available in the app and notification; stopping must prevent later playback, including when preparation or engine initialization finishes. For a target outside the queue, let old audio finish while loading; the session remains owned, and a failed or five-second timed-out load leaves a resumable paused cursor without replay.
15. [ ] **TTS-001 — Text to speech** — Choose Speak on a text message, pause/resume, change rate, move one sentence or one message backward/forward, and stop. Enable Speak over playing media, start read-aloud with other media stopped, then repeat while music or a podcast is playing. → **Expected:** The compact top transport has a single control row plus message progress and estimated seconds remaining, with no message-title/detail rows. Tapping blank transport space returns to the active message. The current sentence is fully visible; when its bounds leave the viewport, follow-along places it at the top. A direct drag suspends movement for the current sentence, while the next spoken sentence resumes following. The correct content is highlighted/spoken, transport remains accessible across chat/list navigation, and Stop clears playback. With no other media, read-aloud starts normally with ordinary audio focus. With other media playing, speech mixes at the selected level without controlling that playback. At 200% RTL and short window height, horizontally scroll the compact control row to every action; the first chat row remains visible and operable below it.
16. [ ] **MSG-015 — Locale/time rendering** — Change device 12/24-hour and app language settings, then use a narrow window, largest font, and RTL to inspect document-card time, delivery state, warning, and the existing edited/reaction labels. → **Expected:** User text is unchanged; app-owned date/time/copy follows locale; the warning precedes the trailing time/status; and the single footer stays inside the last document card without clipping, overlap, duplication, or ambiguous ordering.
17. [ ] **MSG-016 — Report message** — In a group, long-press another member's message and choose Report; pick each reason once, add an explanation longer than 1000 characters, and send while online and while offline. Repeat on Alice's own message, a deleted message and a message in a direct chat. → **Expected:** Report sits last in the action menu and only appears on another member's live message in a group; own messages, tombstones and direct chats never offer it. The sheet lists Spam, Nudity or sexual content, Malware or scam link, Abusive language, Illegal content, Impersonation and Something else, states that the report goes to the group's admins encrypted and names the reporter, and caps the explanation at 1000 characters. Online, the confirmation says the report was sent; offline it says it was saved and will be sent when the chat is back online; a member who has been removed sees that reports are not possible here. Every member then sees a Reported marker in that message's footer, and Message Details lists each report with its reason, explanation, reporter and time. An admin can dismiss an open report, after which it shows as Dismissed for everyone; non-admins see no dismiss action.

### Group, invite, profile, and membership flows

1. [ ] **GRP-001 — Group details** — Open prepared group details and scroll through all sections. → **Expected:** Name/avatar/emoji and description identify the current group. After the header actions, the content order is Members, Edit group and Add member, Files, Advanced, Relays, remaining group/lifecycle settings, then Developer tools at the very bottom; developer panels follow that row when developer mode is on. Check the large identity avatar, copyable full key, About/Mute/Disappearing/Search actions and five-member preview. Scroll down, open About, return, and verify the same viewport and compact identity header. Repeat with a second group and at 200% text/RTL. See all opens the full Members screen; search/role/progress/pending invitations remain native. While deep in that screen, trigger a controlled mutation failure: its error stays visible, Copy retains the safe report, and Dismiss clears the current error. Transfer-admin confirmation remains available. Open Relays and copy a full relay value; an empty group shows its genuine empty state. Back from both routes preserves overview position. Every Advanced (or Chat actions) row carries its own leading glyph — collapse, read aloud, notifications and bubble colors — aligned in one column across the switch and chevron rows, dimmed with the row when it is disabled, and mirrored in RTL only for the collapse and read-aloud glyphs; TalkBack still announces each row once.
2. [ ] **GRP-002 — Edit name/description** — As admin edit name/description, cancel once, then save. → **Expected:** Validation prevents invalid input; cancel preserves old values; save updates header/list/details on every member device. The separate editor uses the shared Settings header and retained bottom Save action; preserve the exact name/description draft when canceling avatar selection or dismissing the keyboard. After a successful rename, return to Chats without restarting: the row, its search and folder results, its archived entry and its sort position all read the new name straight away, and a rename that fails leaves the old one everywhere.
3. [ ] **GRP-003 — Group image/emoji** — Set, replace, and remove a group image or emoji using available picker/search actions. Pan, pinch and turn the crop before accepting, and cancel one crop without replacing the current image. → **Expected:** Preview is correct, cancellation is non-destructive, and the final avatar synchronizes without stale cached images. The masked preview and the stored group image show the same square; a web image keeps its existing path, and a picture that cannot be read falls back to the uncropped upload rather than opening an empty crop.
4. [ ] **GRP-004 — Non-admin restrictions** — Open the same group as non-admin Carol. → **Expected:** Admin-only edit/member actions are absent or disabled with explanation; Members still leads directly into Files, the remaining group settings retain their order, and Developer tools stays last. Ordinary messaging/details remain available.
5. [ ] **GRP-005 — Add member** — As admin add a disposable fourth identity, cancel once, then confirm. Repeat with an authoritative 49-member roster and select one then two new identities; also open Add Members on an authoritative 50-member roster. → **Expected:** Search/selection excludes existing and pending members, confirmation changes membership once, and all devices show the new member/system event. No large-group warning appears for 48 existing members plus one selection. At a projected 50 or more, a persistent warning says additional invitations might not be delivered reliably; Confirm opens a second explicit Continue/Cancel choice, Cancel preserves the selection, and Continue submits once. A 50-member group shows the warning immediately on entry. Duplicate selections and the active account count once, selection changes update the warning at the threshold, and an unresolved or stale roster shows no warning until the authoritative roster arrives. The picker, selection and open confirmation survive configuration recreation. Verify the warning and dialog in light, dark and AMOLED modes, at 200% text, in RTL, and with TalkBack.
6. [ ] **GRP-006 — Remove member** — Remove the disposable member, cancel once, then confirm. Watch the removed account's own notification shade for the removal card, both with the app running and after a cold start, once with the removing admin already in that account's contacts and once with a profile it has never seen. → **Expected:** Scope is explicit; cancel preserves membership; confirm removes exactly that member and updates access/state on all devices. The removed account's card appears immediately without waiting for any profile lookup, says who removed them only when that admin's name is already known locally, and otherwise keeps the direct wording — it never reads "Someone removed you", a raw key, or an unresolved placeholder, and one removal keeps one card.
7. [ ] **GRP-007 — Promote/demote admin** — Promote Bob, verify Bob's controls, then demote Bob where allowed. → **Expected:** Role badges/actions synchronize and the last-admin invariant cannot be violated.
8. [ ] **GRP-008 — Transfer admin** — Transfer Alice's admin role to Bob, cancel once, then confirm. → **Expected:** Warning names the effect, cancel preserves roles, and confirmation atomically changes the available controls.
9. [ ] **GRP-009 — Leave group** — As a non-sole admin leave a disposable group, cancel once, then confirm. → **Expected:** Cancel preserves membership; confirm removes the group or marks it left and prevents further send while preserving honest local history behavior. The confirmation names the group and its body states that leaving removes the account from the group and that a new invitation is needed to rejoin; it never describes muting or stopping notifications.
10. [ ] **GRP-010 — Sole-admin leave/delete path** — As sole admin attempt to leave, choose a successor or destructive option as offered. → **Expected:** The app blocks orphaning the group, clearly separates transfer from deletion, and applies only the confirmed outcome.
11. [ ] **GRP-011 — Invite preview** — Open a pending invite from Chats/notification. → **Expected:** Inviter, proposed group, known members, and Accept/Decline actions are visible without exposing unrelated account data.
12. [ ] **GRP-012 — Accept invite** — Accept on the intended account while online, then reopen it from Chats and its notification during catch-up. Repeat while group details are refreshing; also check a genuine later re-invite after removal. → **Expected:** Progress is non-duplicating, one group opens/appears, and membership/message access become available. An accepted invitation does not return to Join/Decline as older updates arrive. Conflicting initial snapshots wait for local confirmation with a retry on failure; they never accept a new invitation automatically. A genuine later re-invite still requires Join.
13. [ ] **GRP-013 — Decline invite** — Decline a disposable invite, cancel once if confirmed, then complete. → **Expected:** Cancel preserves the invite; confirm removes only that invite and it does not open as joined.
14. [ ] **GRP-014 — Invite failure/retry** — Attempt Accept offline, restore networking, and retry. → **Expected:** The invite remains pending through failure, reports a recoverable error, and retry resolves it once.
15. [ ] **GRP-015 — Mute durations** — Test each preset and a custom date/time, including invalid/past input, then unmute. → **Expected:** Selected expiry is displayed accurately, invalid input is blocked, and notification behavior changes/restores at the expected time. Open the mute-duration picker, then switch to a different conversation/controller: the old picker closes and cannot mute the replacement chat.
16. [ ] **GRP-016 — Conversation notification overrides** — Change notify level, vibration pattern, and other displayed per-conversation overrides. → **Expected:** Selection persists, summary reflects it, and a synthetic qualifying/non-qualifying event follows the override.
17. [ ] **GRP-017 — Disappearing messages** — Choose Off, a preset, and custom duration in a disposable group; send messages and wait/advance to expiry. → **Expected:** Current policy is visible, only messages under the active policy expire, and expiration removes content consistently without affecting newer exemptions. After a message expires, searching for its text from inside the conversation and from the chat list returns nothing, and its attachments no longer appear in the shared-media browser, even before the next hourly prune. A conversation with no visible messages and a policy in force explains the policy and its duration rather than showing the plain empty state, updates when the policy changes, and never claims messages were lost. The disappearing-messages explainer states that the selected duration applies to new messages whether they have been read or not and that expiry permanently removes them for everyone; it never says messages are deleted after being seen or read.
18. [ ] **GRP-018 — Group media** — Open shared media from details, switch each displayed media category, and open a result. → **Expected:** Only that group's media appears, empty/loading/error states are honest, and Back returns to details. Open Photos & videos, Links, Documents and Voice separately, including empty categories. Counts and empty text describe loaded chat messages. In Photos & videos switch All/Images/Videos and check independent scroll restoration and month headings. Go to message opens the exact source row; account/runtime changes and confirmed deletion remove unavailable viewer pages without exposing old content.
19. [ ] **GRP-019 — Group TTS auto-read** — Change the displayed auto-read choice and receive matching/nonmatching messages. → **Expected:** Summary persists and only messages covered by the selected policy are spoken.
20. [ ] **GRP-020 — Group state while offline** — Open details offline and attempt a membership/settings mutation. → **Expected:** Cached state remains readable, mutation does not claim success, and retry after reconnection produces one update.
21. [ ] **GRP-021 — Group recovery and rejoin** — Open a disposable recovered-branch group with automatic recovery failure, pending/failed re-invites, and two rejoin invitations. Interrupt one status read and retry it. Review each authenticated inviter. Cancel once, decline one offer, then confirm the other; repeat after changing its local group state while the confirmation is open. On a disposable group, also use Reset and wait for a new invite from Group details, cancel once, confirm once, then have an admin re-invite the account. Separately, on GrapheneOS, create a fresh group and open it immediately from the creation flow, then background and foreground it, rotate or otherwise recreate the activity, and drop and restore the network while it is open; keep a seeded genuine-recovery group available in the same account and open it afterwards. → **Expected:** The newly created group stays usable with no recovery notice and no Retry, through backgrounding, recreation and the network drop, while the seeded recovery group still shows its real recovery state and actions. A failed status read keeps prior evidence inspectable and offers Retry. Each review action identifies its inviter and epoch before opening the confirmation. Advisory recovery failure never changes membership or disables sending. Pending retries need no host publication; exhausted retries direct the tester to invite again. Decline removes only the chosen Welcome. Rejoin explains that this device’s group encryption data is replaced while message history stays, submits the exact reviewed Welcome ID and state token once, refreshes members and timeline, and rejects stale approval until the refreshed offer is reviewed. Recovery notices use centered bounded surfaces; long trust warnings, Retry and invitation review/decline remain reachable in dark/AMOLED, RTL and 200% text; the reset asks for confirmation, removes the group's history and media from this device without sending a leave or disband, reports when the group was already waiting, and the group reappears only through a fresh invite created after the reset.
22. [ ] **GRP-022 — Save transcript** — With Developer tools enabled, open a disposable conversation’s details and use Save transcript. Cancel the document picker, then choose a destination and save. Compare the JSON with the native Share transcript export. Repeat a cancelled/failed provider write and switch account or conversation while the picker is open. → **Expected:** Cancellation produces no export; a successful file contains that conversation’s accepted native transcript once. Progress prevents duplicate saves and competing exports. Failed writes show recovery feedback and remove or truncate incomplete output where the provider permits. An old picker cannot export a replacement account/chat, and temporary private plaintext is cleaned up.
23. [ ] **GRP-023 — Authenticated system rows and prepared names** — In a group where Alice has no cached profile for one member, have an admin add, remove and rename members, and have a non-admin member send a payload shaped like a system event through a compatible client. → **Expected:** Membership, admin and rename rows appear only for changes MarmotKit authenticated from group state; the member-authored payload renders as an ordinary message and never names an actor or subject. Where Alice has a nickname or cached profile for a participant that name is used; where she only has an npub or raw key, the row shows the display name MarmotKit prepared from the authenticated projection instead. Notification text for the same events follows the same rule.

### Profiles and account keys

1. [ ] **ACC-001 — Profile sheet** — Open own, known-contact, unknown, and group-member profiles from every available entry point. → **Expected:** The same identity is shown consistently with correct name/avatar/`npub` and relationship/actions. The person profile opens full-screen with the pinned Message action and current member/admin role where supplied. On a short/large-text/RTL window all public fields and available actions remain reachable. Unknown follow state stays disabled; no fixture relay count or unsupported block switch appears. For a designated contact, exercise Follow and Unfollow, then repeat with a native failure: the prior relationship remains and the profile shows recoverable failure feedback without claiming success. On the Welcome/Add Profile entry, Sign In and Sign Up stay at the bottom above system navigation in light/dark modes and short windows; both remain reachable with large text.
2. [ ] **ACC-002 — Profile actions** — Copy `npub`, open QR, message/add-to-group where available, and dismiss. Also block Bob from his profile, try to message him, then unblock him. → **Expected:** Copied/QR identity decodes to the shown account; actions use the active account and return correctly. Check the New Message heading capitalization. From New Message, open Connect with QR and check that Share & Connect shows the public profile code and its Scan QR Code action. The separate Show my QR code entry is absent; Back preserves the recipient query, scanner cancellation stays on Share & Connect, and a valid public-profile scan returns to the recipient flow. Copy the public identifier capsule and verify the exact displayed npub, two-second copied feedback and no private nickname/notes in its contents. When a Lightning address is displayed, copy it and verify the exact address. Back returns to the original caller; account/runtime replacement revokes captured callbacks; Block switches to Unblock only after MarmotKit confirms, a direct message to a blocked person is refused with an explanation while shared groups keep working, and an unconfirmed block publication is reported as not yet confirmed rather than as success.
3. [ ] **ACC-003 — QR scanner** — Open scanner, deny/grant camera, scan valid `npub`/supported QR, malformed QR, and cancel. Include an nprofile QR that carries relay hints. → **Expected:** Permission is contextual; valid data opens the intended profile/action once; malformed data is rejected safely; cancel changes nothing. Check the near-full black scanner, centered Scan QR Code title, Close, rounded target, and torch only on flash-capable cameras. Toggle torch, deny camera once and permanently, return after changing permission in Android Settings, rotate, background/resume, cancel and reopen. Native camera failure shows a dialog with its real reason, Retry and Close and no pretend target; Retry binds a fresh camera, while Close, outside dismissal or Back cancels the scanner without delivering a result. The permission card uses Allow camera or Open settings as appropriate. No preview opens without a grant; cancellation stops camera/torch use and ignores late scan results. Verify recovery remains reachable at 200% text in RTL/landscape and short windows: scroll the permission card to its complete 48dp-or-larger Allow camera/Open settings action below the header. Tiny targets are omitted without removing the real camera preview; npub and nprofile payloads are validated by MarmotKit, an nprofile with relay hints opens the same profile as its npub, and a checksum-invalid code is rejected.
4. [ ] **ACC-004 — Full-screen avatar** — Tap local/remote/missing avatars, zoom if supported, rotate, and Back. → **Expected:** Correct image or deterministic placeholder appears, failure does not loop, and Back restores the source. Person Profile preserves production banner/avatar loading and failure collapse. Open an available image then Back; the full image and nested private/feedback dialogs retain the caller's secure-window policy.
5. [ ] **PRO-001 — Edit profile text** — Open Profile in read mode, then Edit. Change name, verified address, Lightning address and About; Back discards before a second Back leaves. Save once, including invalid and unresolved Lightning and publish-failure cases. Repeat while refresh completes and while switching accounts. Tap the name-field dice several times, then Restore saved name; also try Restore with the field untouched and on an account whose saved name is blank. → **Expected:** Read fields keep full contrast, sources and Save appear only while editing, public visibility is explained, edited fields survive refresh, verified seals match the account and unchanged verified address, failed saves keep the draft, accepted Save returns to read mode, and a single captured complete metadata publication updates local/remote views. Back during Lightning preflight prevents submission; an already submitted native publication can complete and is never presented as rolled back. Stale account work never changes the new account. At 200% font, RTL, and with the IME open, every field and pinned Save remains reachable. Add Profile opens the full-screen prototype Welcome with Sign In and Sign Up. Sign In uses the masked shared field and Amber alternative; public keys remain rejected here. Sign Up closes the entry and opens the shared local Name/About/photo form above the current Chats/Settings route. Back before submission creates or publishes nothing and retains the original account. Submission creates once, accepts the native account and publishes its submitted metadata; a failed photo/publication stage retains that accepted account for explicit retry or Continue without profile, never another creation. Inspect light/dark/AMOLED, 200% text, RTL, short landscape, tablet and IME; both secure windows must block screenshots of private-key entry; each suggestion is a two-word pseudonym from MarmotKit's shared generator and retries when it matches the name currently shown. Restore saved name returns the field to the published name; it stays visible at all times and is enabled only while the field differs from that name, so an untouched field shows it greyed rather than missing. The name-field dice requests a new one, retries a repeated current value, and changes only the draft until Save. Successful publication feedback says the profile is now on the reported number of relays and never exposes protocol event terminology such as “kind:0 metadata.” Save with a profile field still focused and the keyboard open, in both gesture and three-button navigation: the confirmation title and relay count are fully readable above the keyboard for their whole two seconds without dismissing it, the focused field keeps its focus and text, and the pinned Save bar rests on the confirmation rather than floating a keyboard height above it. Repeat the same check for Save with the keyboard closed, and for adding a relay from a focused relay-URL field in Settings, which shares the same confirmation host.
6. [ ] **PRO-002 — Edit profile picture** — In Edit mode open the separate banner/photo menus, choose Photos, Files or web image, cancel once, save, then remove. From Settings home, tap only the active profile avatar, then repeat by tapping the name/key area. For the photo, pan and pinch the crop, turn it, cancel it once, then accept; repeat with a wide and a tall source. Confirm the banner still takes its own wide framing rather than a square one. Switch account while the platform picker or upload is pending. View saved banner/avatar in read mode. → **Expected:** The Settings avatar has its own localized, accessible 48 dp action and opens Profile in edit mode with the picture actions expanded once; the surrounding row still opens Share & Connect and each tap dispatches exactly one destination. Loading waits for the same account's profile before expanding, failure never opens actions over missing content, dismissing the menu changes nothing, and Back exits edit mode before leaving Profile. Source results remain scoped to the account and editing session; image processing, safe URL validation and upload errors remain recoverable; independent uploads block Save; Back discards local image drafts; the avatar published is exactly the square left under the mask, at the zoom and turn chosen, with a cancelled crop uploading nothing; accepted Save/removal propagates without exposing the source file path. View mode opens images without exposing editing sources until Edit is chosen. On a high-density phone, view a genuinely high-resolution 2:1 banner in Profile Settings, in both read and edit mode, and again on another member's profile: it is as sharp as the avatar from the same profile, not soft or pixelated, and its 2:1 crop is unchanged. Re-enter the screen to check the first cached frame is equally sharp, then switch accounts and sign out to confirm the banner clears with everything else and a late result never restores it.
7. [ ] **ACC-007 — Private contact details** — Open any private-details dialog for a controlled contact and edit/clear values if supported. → **Expected:** Private fields are labeled, stay local/private as documented, and never appear in another account's profile. The person profile private editor uses explicit Save/Cancel. Hold a captured Save across account replacement: it must not write into the new viewer's contact record. The hint says the nickname and notes stay on this device, only you can see them, clearing restores the profile name, and nicknames are limited to 80 characters. Saved nickname still follows native single-line/80-character normalization; cancel preserves the previous local values.
8. [ ] **ACC-008 — Add profile to groups** — From Bob's profile and from the settings screen of Alice's DM with Bob, use Start group and Add to a group; inspect groups where Bob is already a member or otherwise ineligible. → **Expected:** Start group and Add to a group are adjacent on both screens. Start group opens the existing new-group flow with Bob selected exactly once. Add to a group opens the same native eligibility picker from either screen; eligible choices are accurate, existing membership is not duplicated, and ineligible groups are omitted or explained. Groups in Common remains available from the profile and includes only actual non-DM groups whose current roster contains both accounts; pending, left, absent-member and unconfirmed rosters never become fake shared rows. An unresolved roster has an explanation and explicit Retry. Archived shared groups remain included. With a conversation foregrounded, hydrate then remove a membership: both routes update even while Chats stays frozen. A known shared group opens its existing conversation; Back returns to the originating profile or DM settings. Partial invite failure remains visible with native safe-report Copy when offered.
9. [ ] **ACC-009 — Profile Keys screen** — Open Settings → Profile Keys with a locally signed account, then repeat with an Amber-signed account. → **Expected:** Public identity, private key, encrypted export and raw export are clearly distinguished; raw key starts hidden; reveal/copy use the selected local native key. Amber accounts show the signer callout without local private-key actions. No new biometric prompt is implied.
10. [ ] **ACC-010 — Copy and share keys** — Copy the public identity and, on a disposable account, deliberately reveal/copy the raw key. Open raw Export and check Cancel, Export nsec and Share independently. → **Expected:** Public copy is correct; raw copies and shares carry Android sensitive metadata. Share opens the chooser only after explicit confirmation and sends the selected account’s raw key; Export requests a document instead. Cancellation, backgrounding, departure, owner/runtime/key loss or teardown discards pending native results. Reveal hides after 30 seconds or earlier departure. Keep evidence free of secrets.
11. [ ] **ACC-011 — Encrypted backup create, view and export** — On a disposable account try empty/mismatched passwords, then matching passwords. Test Export directly, then View backup → Copy, Export and Hide independently; background once, wait for the 30-second expiry once and return from a file picker after expiry once. → **Expected:** Invalid input cannot request a backup. Both password fields clear and disable as soon as native encryption begins. Direct Export opens a document picker; View backup displays the native ncryptsec in a secure dialog without opening a destination. Copy puts the exact ungrouped value in the sensitive clipboard; Export from the preview uses that same result and its original 30-second deadline without re-encrypting. Hide/dismiss/30 seconds/background/departure/owner/runtime/key loss/teardown clears the temporary preview. An ordinary picker stop/start retains an authorized pending file result, but expiry/departure/owner change/teardown prevents writing and never regenerates it; expiry is reported safely. Test failures allow a deliberate retry with a fresh passphrase.
12. [ ] **ACC-012 — Encrypted backup import limitation** — On the sign-in surface enter the copied ncryptsec value from ACC-011 and attempt import. → **Expected:** The app reports a safe import failure because encrypted-secret-key import is not yet supported, creates no account, requests no restore passphrase, and does not claim that the backup was restored.
13. [ ] **ACC-013 — Sign out** — Sign out Alice without wipe, cancel once, then confirm. → **Expected:** Cancel preserves session; confirm shows non-dismissible progress until teardown finishes and removes Alice from active UI while retaining only the explicitly described local account data for continuation. The sheet separates data kept on this device from what changes, says notifications stop and temporary app data is cleared, and presents removing invitation keys from relays accurately as a choice controlled by the default-on toggle. While teardown is slow, repeat Sign Out, Switch Profile and Back: no second teardown or profile navigation starts; failure clears progress and keeps the correct account available.
14. [ ] **ACC-014 — Sign out and wipe** — On a disposable account choose sign out and wipe, cancel once, then confirm and wait through completion. → **Expected:** The confirmation names what is deleted — local messages, group-encryption data, the stored sign-in key and published invitation keys — and explains that signing in again cannot restore prior groups, messages or media on this device and that new group invitations are required. Progress cannot be double-started, result reports success/failure honestly, and wiped account data/drafts/notifications do not reappear after relaunch.
15. [ ] **ACC-015 — Multi-account isolation after wipe** — Wipe Carol while Alice remains saved, then switch/relaunch. → **Expected:** Alice is intact; Carol is absent; no Carol notification, draft, folder, profile cache, or media remains visible.
16. [ ] **ACC-016 — Key packages** — Open Key packages, refresh, and exercise each displayed publish/delete/recover action on a disposable account; use ACC-018 for focused deletion coverage. Open the Relay history section after publishing a new package. → **Expected:** Current/pending/error state is labeled, mutation progress is singular, and refresh reflects the authoritative outcome. Publishing uses Republish and a full-width Publish New Key Package action. Published records show source relays and copyable ID, event and ref; Retained Local Material is listed separately and is never a relay-deletion target. Cancel deletion and verify no native write; confirm and verify the list changes only after acknowledgement. Repeat with retained-only and empty inventories, malformed event IDs, a slow refresh, 200% font/RTL, and AMOLED. Switch accounts during a load/deletion and verify no old-account result reaches the new screen; Relay history marks the current slot winner and lists superseded relay events with their source relays.
17. [ ] **ACC-017 — Account-bound settings** — Give Alice and Bob different relay, notification, folder, and privacy-related choices, then switch repeatedly. → **Expected:** Account-scoped values follow the account and device-scoped values remain device-wide as labeled. Quick account switching is app-wide and defaults on. Switch/add/sign out accounts and relaunch: it stays enabled. Turn it off: the cycle disappears, the normal selector remains, and the off choice survives account changes and relaunch. Only approved disposable/emulator Erase App Data checks reset it to on; never erase a personal device for this test. Switch accounts while a folder editor or delete confirmation is open. The old editor/confirmation closes and its callbacks cannot save, delete, reorder or restore folders for either account after the switch. A new editor for the next account starts from that account’s own folder data.
18. [ ] **PRO-003 — Follow and unfollow** — Open Bob's profile, follow, verify the relationship on both profile and search surfaces, then unfollow; repeat one mutation offline. → **Expected:** Progress is singular, successful state is consistent across surfaces, and failure retains the prior relationship with a recoverable error. The grouped Follow/Unfollow row preserves one native mutation, unknown-state disablement and the prior relationship on failure. Native feedback is visible above the full-screen profile; stale callbacks cannot acquire a replacement account.
19. [ ] **PRO-004 — Nickname and private notes** — Add, edit, cancel, save, and clear a synthetic nickname and private note for Bob. → **Expected:** Cancel preserves old values, saved values are visible only to Alice where labeled, clearing removes them, and Bob/Carol never receive the private fields. Verify full-screen profile → Nickname and notes → Cancel/Save/clear under IME, large text and rotation. Published name remains distinct from the local nickname; private notes never appear in the public profile or public-key clipboard.
20. [ ] **PRO-005 — Contact sharing and conversation shortcuts** — From Bob's profile exercise Message, Invite to White Noise/share chooser, Start new group, and Add to group across eligible, already-member, empty, loading, and failure states. → **Expected:** Existing DM is reused, sharing occurs only after chooser confirmation, new-group selection contains Bob once, and group eligibility/progress/errors are accurate. Pinned Message reuses canonical native DM resolution and actual retry-group receipt. Start group passes the existing single Candidate callback to the unchanged group owner. Add/promote/admin failures preserve native error/report delivery above the profile. No prototype fake messages or relay-count gating is used.
21. [ ] **ACC-018 — KeyPackage local-first inventory, deletion safety and recovery** — Use only disposable accounts and a relay-backed KeyPackage with a valid event ID and public `wss://` source; include an external Outbox source when available. Open the screen with network blocked, then refresh online and once with a controlled refresh failure. Cancel deletion and confirm the event remains, then confirm Delete once and refresh, reopen the screen, and restart normally. With another disposable target, try offline deletion and retry after reconnecting. If two test accounts are available, switch from A to B during a delayed deletion and return to A to refresh. → **Expected:** The first render comes from MarmotKit's synchronous local inventory without network and labels the typed local state (not local, current, pending replacement, retained private material, or other owned material). Explicit Refresh performs the network read; failure preserves the previously loaded rows and shows an error instead of replacing them with an empty inventory. Cancel does not mutate; acknowledged deletion removes that exact published event after refresh/restart, while any replacement has a different event ID. Failure is bounded and retryable, never false success; DNS-verification or no-safe-source errors, if encountered, preserve the target and show recovery feedback. A late A result never changes B's inventory. Local-only retained material does not offer a misleading relay-delete action, and retained private material is not deleted. Do not delete a depended-on account's last usable published KeyPackage or treat a relay acknowledgement as proof of erasure from every relay. Controlled DNS/no-safe-source fixtures are automated coverage, not hidden manual setup steps.
22. [ ] **ACC-019 — Share profile and app-invite QR cards** — From Settings → Share & Connect, open Share and exercise Share profile URL and Share profile card. Repeat the card path from the profile QR sheet, then use Invite friends from New Message. Save/send both PNGs to a cooperative external app and scan each from another screen. Repeat the profile card with and without a cached avatar, a long name, RTL text, dark/AMOLED mode and 200% app text; cancel and reopen the chooser, then switch profiles or leave the surface while a card is being prepared. → **Expected:** URL sharing preserves the existing public profile URL. The profile PNG stays a fixed light card with the current visible name/avatar or initials, White Noise branding, and a QR that decodes exactly to the canonical profile `qrUri` including `from=qr`. Invite friends shares a branded PNG whose QR decodes exactly to `https://www.whitenoise.chat/download` and whose visible fallback remains the localized invitation text; if card preparation fails, the existing text-only invite chooser remains available. Both intents are `image/png` with a read-only FileProvider stream, no neighboring cache access, and no relay hint, account hex, raw key, diagnostics, private note or contact state in pixels, filename or PNG metadata. Cancelling changes no app state, and a stale account/runtime/surface never opens a late chooser. Do not uninstall or clear app data for this test.

### Settings and customization

1. [ ] **SET-001 — Settings inventory** — Scroll Settings from top to bottom. → **Expected:** The profile header (name, short key, QR glyph) opens Share & Connect (avatar, name, address, copyable key capsule that reads Copied for two seconds, black-on-white QR card, "Scan to connect.", pinned Scan QR Code that opens the scanner and presents a scanned profile, or explains an invalid code under the caption; the top-bar share action sends the profile link); Add Profile appears with one signed-in profile and Switch Profile with several; App updates appears only in self-managed builds, and only leads the screen while a newer version is actually available — with nothing to install the same row sits quietly above the version footer, without its emblem, still tappable to check; then Profile, Profile Keys (public key row with copy, masked private key with show/hide that re-hides after 30 s, Copy Private Key, Export Encrypted Private Key with a two-field password dialog and strength meter, Export Private Key with a consequence dialog, both writing a file you choose; Amber accounts see a callout instead; Sign Out & Wipe as the last destructive row), AI Agents, Notifications (until Android allows notifications: an Allow notifications action, or after a denial a Notifications are off link into Android settings; Delivery group with one Local notifications or Push notifications choice and one link per Android notification category), Read Aloud, Dictation, Appearance, Folders, Privacy & Security, Data Usage, Relays; Help, Chat with support, Donate, Developer Tools; Sign Out; and the version footer. Key Packages lives under Developer Tools. Back returns to Chats. Appearance begins with the on-by-default Quick account switching whole-row toggle and explanatory copy; turning it off does not activate another account and preserves the normal selector.
2. [ ] **SET-002 — Theme modes** — Select System, Light, Dark, and AMOLED; relaunch after each. → **Expected:** Selection applies immediately and persists; AMOLED uses true-black principal surfaces without unreadable borders/text, and its transient surfaces (snackbars, media badges, attachment-remove buttons) are black with white content rather than a grey panel.
3. [ ] **SET-003 — System theme** — Choose System and toggle Android light/dark while White Noise is foreground/background. → **Expected:** White Noise follows the system without losing the current route or flashing the wrong theme.
4. [ ] **SET-004 — Action color** — Tap presets, drag the hue/saturation/brightness sliders and type a hex value; watch the pinned Preview button, press Back once without saving, then reopen, change the colour and Save; finally Reset to default and Save. → **Expected:** Save stays disabled until the draft differs or a hex is invalid, Back without Save keeps the old colour, the Preview button and, after Save, buttons/links/selections take the colour, contrast remains legible, and Reset restores the monochrome default. Explicitly save white for Light, then Reset to default and Save: Edit/Suggest name, selected theme indicators and enabled toggles return to near-black in Light; Dark retains light actions. Reset without Save must not alter other screens or another theme’s saved color.
5. [ ] **SET-005 — Chat bubble colors** — Change Your messages and Other messages with presets, sliders and hex while watching the pinned preview (received above sent), Save, inspect a real chat, then use the ⋮ menu's Reset to default and Save; repeat from a chat's details where the menu reads Reset to global colors. → **Expected:** Only intended bubbles change after Save, text/status/reactions remain legible in all themes, chat overrides fall back to the account defaults, and reset is complete. Changing the account Action color must not recolor default message bubbles. In AMOLED, the editor remains available, previews pure-black bubbles with the chosen colors on their complete outlines, and applies those outlines to real sent and received messages; the shared per-chat value remains a fill in Light/Dark and an outline in AMOLED. Check default incoming/outgoing text, timestamp and sender contrast, chat-over-global precedence, reset, failed/deleted colors and live account/theme switching.
6. [ ] **SET-006 — Font size** — Select Small, Default, Large, and Extra large, visiting Chats/conversation/settings each time. → **Expected:** Scale applies live and persists; text does not clip, overlap, or hide primary actions.
7. [ ] **SET-007 — App font** — Select every displayed font including System. → **Expected:** Picker previews and app text use the selected family, glyphs/emoji remain present, and choice persists.
8. [ ] **SET-008 — App language** — Select System, English, and at least one non-English locale, relaunch, then return to System. → **Expected:** App-owned text changes consistently, user content/identifiers do not, and selection persists.
9. [ ] **SET-009 — Chat folders CRUD** — Create a folder from the top-bar action, name and describe it, add Included Chats and People, set a keyword and the four rule switches while watching Preview, Save, then edit it, use the row menu's Move Up/Move Down and Delete, and press Back from a dirty draft. → **Expected:** Save stays disabled until the name has text; Preview counts what the draft matches; Back offers Discard Changes or Keep Editing; the menu moves and deletes the exact folder; chips and order follow; deletion does not delete chats; Restore Default Folders is enabled only while a default is missing. The rules hint says every selected rule must match and that a chat removed by hand returns if it still matches. Rows use native tap-to-edit and long-press actions; New Folder, Edit Folder, Included Chats, Move Up/Move Down and Restore Default Folders follow the new layout. Rename a default and verify its stored name appears in the list/editor; an untouched default keeps following the app language. Move a populated/rule-based folder, reopen the app, and verify IDs, description, memberships, rule and order survive. Rotate with unsaved chat/person selections, then Save; selections remain in the draft until Save. Repeated Save creates at most one folder. Opening initializes legacy folder defaults before editing; Save requires initialized account state and never seeds/migrates. Name, description, membership and rule use one existing preference edit/state publication; validation or transaction failure retains the draft for Retry, and no synchronous durable-disk claim is made. A folder removed before Save reports unavailable and retains the draft. Keep Editing returns to the dirty form; Discard changes no folder data. During sign-out, wipe or runtime replacement, captured Save/Move/Restore/Delete actions must not recreate cleared preferences. Check light/dark/AMOLED, large text and RTL.
10. [ ] **SET-010 — Data/storage inventory** — Open Data and storage and exercise every displayed network/media auto-download toggle. → **Expected:** Summaries match selections and choices persist after relaunch/account switch according to scope. On a fresh profile and after Reset download settings, no media type lists Metered network: on a metered connection a received photo shows its Download affordance instead of fetching, already cached media still renders, and Download still works. Turn Metered on for Photos and relaunch: that choice survives and photos fetch again on the same connection.
11. [ ] **SET-011 — Auto-download behavior** — Send each media type while recipient is on allowed then disallowed network conditions. → **Expected:** Allowed types download automatically; disallowed types remain manual with size/type information; changing policy affects future work.
12. [ ] **SET-012 — Stop automatic downloads** — Queue multiple safe downloads, use Stop automatic downloads, confirm/cancel, then resume manually. → **Expected:** Cancel keeps queue; confirm stops only automatic work, communicates backlog state, and manual retrieval remains possible.
13. [ ] **TTS-002 — Text-to-speech settings** — Open Read Aloud, change Engine, Voice and Speech rate (including Custom), toggle Read messages aloud by default and Speak over playing media, pick a Speech-over-media volume, then use Refresh and Open Android Settings. → **Expected:** Each picker is a dialog whose rows name why an unavailable voice cannot be chosen; a custom rate outside the supported range is refused; an external engine asks for confirmation before it is selected; the discovery callout tells the truth about engines and offline voices; Android's settings open or the failure is stated. With a saved voice whose engine no longer exposes a voice catalogue, Voice remains available and Automatic clears that engine's saved choice. A working engine without an optional catalogue must not show No offline voice available; a populated catalogue containing only unavailable voices must still show that warning.
14. [ ] **DIC-001 — Dictation disclosure** — Start dictation for the first time, cancel disclosure, then accept and dictate synthetic text. → **Expected:** The first-use text explains that the installed speech service processes the audio and may send it to its provider, that White Noise keeps the returned text only until you Paste or Send, and that automatic sending stays off until you turn it on. No audio leaves the device before consent, cancel starts nothing, and accepted dictation is delivered only by the explicit Paste or Send action.
15. [ ] **DIC-002 — Dictation settings/results and background controls** — Confirm Settings offers no “When finished” choice while finishing stays manual, then set Finish dictation to a silence interval and confirm the choice appears offering Paste into draft and Send message, and retracts again when finishing returns to manual. Exercise each displayed dictation mode/provider/language option plus cold first-tap, provider-disconnect/app-permission recovery, dictation started while Android's global microphone privacy toggle is off, finish/cancel/error paths, at least two minutes of manual silence followed by actual speech, and one continuous 60-minute synthetic dictation that crosses repeated recognizer restarts before finishing with Done. Paste and Send are chosen per dictation and Send is the composer's own arrow-up disc; with the silence choice left at Paste into draft, let a dictation finish on silence without touching either and confirm it lands in the draft, then set that choice to Send message and confirm the same silence finish sends. Finishing with Done must land in the draft whichever way that choice is set. Upgrading from a build that had the old app-wide “send when finished” stored must start at Paste into draft rather than inheriting it. During capture, navigate Home and another chat, open that chat's keyboard, then use each persistent or notification action (Cancel, Paste, and Send) before returning to the origin conversation. Also try Stop, Paste, and Send during a provider-error retry and revoke microphone access before the retry fails. With an app-audio provider, stop just after a 30-second chunk boundary and check that the final words are retained; force an empty final and a temporary provider disconnect for the last sealed chunk, then verify White Noise retries that exact chunk and never delivers only the earlier text. Repeat the empty final three times after pressing Paste and Send on separate long transcriptions. Enable three-second silence completion with the silence choice on Send message, speak a short phrase below the provider's minimum chunk, then wait without tapping either action; repeat, tapping Paste while the phrase is still being processed. Start again, pause for about 2.5 seconds, resume speaking, and then leave three full seconds of silence. Also speak continuously across two chunk boundaries and wait for automatic completion. Cancel repeatedly while a chunk is being processed and confirm the app stays open. With a slow app-audio provider, let the audio backlog fill across a recognizer restart; verify dictation shows the speech-service backlog error and does not silently send an incomplete transcript. While TTS is speaking and again while a voice message is playing, start dictation and finish via Done, Cancel, failure, Paste, and Send; repeat by stopping just after a 30-second chunk boundary before the replacement recognizer is ready, and confirm the microphone indicator clears and the exact playback resumes before the sealed tail result appears. Repeat with playback already user-paused, then replace the paused item/session before dictation ends. Send immediately after a dictated result appears with the keyboard left open, then repeat while dismissing it and with rapid consecutive sends. → **Expected:** Active app playback pauses before microphone capture and only the exact playback interrupted by dictation resumes at its retained position as capture ends, before transcript validation, insertion, or send. User-paused or replaced playback never starts. Persistent Cancel/Paste/Send remain visible and reachable above the keyboard, including large text and RTL; no Pause control is shown. The former sweeping hairline is absent. A compact animated three-bar pulse remains visible while capture is open, including provider-internal chunk transcription and recognizer restarts. Internal chunk work never claims a Paste or Send spinner and never blocks either action. After explicit completion begins, the pulse disappears and one compact circular spinner replaces only the selected Paste or Send icon. In-app mode stays on native controls for every provider that can transcribe audio White Noise captures, a 60-minute recording remains continuous across recognizer restarts and Done returns the complete ordered transcript without duplicated or missing segments, and an empty or transiently failed chunk is retried rather than acknowledged or omitted. Recovery is bounded and remains cancellable; after two retained-chunk retries, another blank, no-speech, or temporary-disconnect final ends the spinner in a retryable failure without pasting or sending the incomplete accumulated transcript. A successful retry drains the retained audio and performs the action that was tapped exactly once, or the stored silence choice when the quiet interval ended the session. Silence does not end manual dictation. With automatic completion, startup silence never finishes an empty session; the timer starts from actual caller-audio speech, resets when speech resumes, then seals and drains the complete short or multi-chunk tail after the configured quiet interval and performs the stored silence choice exactly once; that choice governs automatic completion alone, so Done still lands in the draft and a pressed Paste or Send overrides it. Each action changes only the immutable origin draft/chat, any unrelated current conversation remains unchanged, reopening the origin shows the intended result, Cancel preserves the prior draft, and failures provide recovery without sending text. Provider microphone rejection with White Noise access already granted is identified as a speech-service failure, not a request to grant White Noise permission. After microphone rejection, completion during a failed retry preserves that cause instead of reporting no speech; any accumulated text is automatically preserved in the latest draft without being sent. If the origin conversation disappears or authoritative target validation fails after recognition completes, the nonblank transcript remains in a retryable failure without writing any draft, recreating the old draft, or reaching another conversation. Explicit completion during a temporary disconnect never silently commits earlier text while a caller-audio tail remains pending. When Android's global microphone privacy toggle is off, White Noise stays on its current dictation surface and Android presents its native microphone-unblock prompt; accepting it continues dictation without requesting White Noise's already-granted app permission again. Actual app-permission loss is identified separately. A missing or refusing speech service offers to open that service's own app instead of a retry that cannot succeed, because Android 17 routes the voice-input settings action to the digital-assistant role, which cannot select a recognition-service-only provider. Paste dismisses dictation after the origin draft write succeeds. Send empties the composer and dismisses dictation as soon as its pending bubble appears; the conversation remains pinned through the final input/IME geometry and the outgoing bubble stays fully visible above the composer, while later delivery progress or failure remains on that bubble. A failure confirmed before dispatch automatically preserves the captured text in the latest draft; if dispatch began but delivery cannot be confirmed, White Noise shows Delivery not confirmed without duplicating the text in the draft. While replying to a message, complete a dictation with Send and confirm the transcript sends as a reply to that exact selected message, then cancel the reply or switch conversation/account mid-dictation and finish: the stale dictation cancels instead of sending standalone text or into the wrong conversation. Which surface a tap opens is covered by `DIC-003`.
   - **DIC-002 alternating-failure subcase:** While recording, force the same speech-bearing caller-audio chunk through repeated no-speech callbacks, then continue with later audio; separately press Paste or Send and alternate no-speech with an empty final or provider-disconnect callback after a pipe stall has already requeued the final chunk. → **Expected:** Recording bounds retries per exact chunk, acknowledges only the repeatedly rejected chunk, and advances to later audio. After Paste or Send, all failures for the unresolved chunk share one retry budget; exhaustion ends the spinner without delivering partial text and offers one explicit Retry that can drain the retained PCM. The exhausted budget never leaks to a different chunk.
   - **DIC-002 provider-capacity backoff subcase:** After a non-empty segment, choose Paste and Send in separate runs, then force the retained final caller-audio chunk through Provider disconnected followed by Recognizer busy before allowing the same chunk to succeed. Repeat once but cancel during the first delay, and once while making the retained chunk disappear before the delay ends. → **Expected:** White Noise remains on the compact selected-action spinner rather than showing Retry/Cancel between attempts, waits about 500 ms and then 1 second before reopening the provider, and finally pastes or sends the complete ordered transcript exactly once. Cancel starts no replacement recognizer and delivers nothing. A drained tail finalizes the accumulated transcript without opening an empty recognizer generation. Privacy-safe diagnostics name the failure, retry and delay but never transcript text, account or conversation identifiers.
   - **DIC-002 target-loss subcase:** Complete a non-empty Paste and Send while the authoritative membership read succeeds, reports removal, times out, and throws; then remove the origin conversation while a completed segment is awaiting delivery. → **Expected:** Only confirmed membership permits automatic Paste or Send. Removal and indeterminate reads fail or cancel the requested action without exposing an edit pen or requiring another click. Diagnostics identify the selected action, validation classification, paste-write result, and send result without transcript text or conversation/account identifiers.
   - **DIC-002 detached-composer subcase:** In separate non-reply runs, tap Paste and Send and immediately return to Chats before recognition finishes; also start each completion after already returning to Chats. → **Expected:** Completion does not depend on the origin composer remaining mounted. Paste writes only the origin draft, and Send publishes exactly once to the origin conversation. Opening another conversation or mounting a reply composer while completion is being claimed never retargets the transcript. A dictation that began as a reply fails closed if that exact reply composer is no longer mounted; it never becomes a standalone message.
   - **DIC-002 pending-row tail-reveal subcase:** With a multiline composer and the keyboard open, use Send on Finish while the origin conversation remains mounted; repeat after scrolling away from the live tail, and once after leaving the origin conversation before its pending row appears. → **Expected:** A pending row published in the mounted origin conversation triggers the same measured tail reveal as an ordinary accepted send, so the complete outgoing bubble settles above the final composer/IME geometry. A send completed while that conversation is unmounted does not trigger a delayed reveal when it is opened later, and Paste never triggers a send reveal.
   - **DIC-002 latency subcase:** In a verified active conversation, dictate for at least ten seconds, leave a natural half-second pause, continue speaking past 30 seconds, then use Paste and Send while observing the controls and pending bubble. → **Expected:** Caller audio seals at the sentence pause after the ten-second provider-safe minimum or at the 30-second maximum, with exact non-overlapping PCM ranges and the complete ordered transcript. A short chunk rejected as no-speech absorbs following contiguous audio before retrying, so it cannot block or discard later speech. Already-verified membership avoids a redundant MDK read. Paste exits after its conditional draft write; Send exits when the pending bubble renders, without waiting for delivery.
   - **DIC-002 silent-tail subcase:** After speaking a complete sentence, leave only silence in the final caller-audio chunk, choose Paste and Send in separate runs, and wait for completion. → **Expected:** A blank final for a speech-bearing chunk or a transient provider failure retries that exact chunk. A capture-side-confirmed silence-only tail is acknowledged once, and the accumulated transcript completes without waiting for the 90-second drain bound.
   - **DIC-002 in-progress-read tail-retention subcase:** While the recorder is actively blocked in a caller-audio read, play a deterministic spoken phrase into the guarded Pixel 6a microphone and immediately choose Paste; repeat with Send. → **Expected:** The selected action retains the in-progress read plus five bounded 100 ms recorder-tail reads, then waits for both provider-feed closure and capture closure, retains the complete distinctive final phrase, and completes exactly once. Cancel remains immediate. Ordinary mid-session provider results or errors continue dictation without stopping capture, and no completion path exposes a second edit action.
16. [ ] **SET-016 — AI agent connectors** — Open AI Agents, open each connector, read its setup prompt, copy one, close the sheet, then copy the public key and open the connector documentation. → **Expected:** Each sheet is titled for that agent, states that the prompt includes your public npub but never your private key, that the agent asks before installing anything, and that you should use only an agent you run and trust, and shows the prompt with Alice's current `npub` and no placeholder; Copy prompt confirms beside the button; the manual row shows the shortened key, copies the complete one and says so; the documentation opens externally and the screen states whether the hand-off started; Back returns to Settings and nothing was installed or sent.
17. [ ] **SET-017 — AI agents without account** — Reach the surface without an active account if possible. → **Expected:** An error callout explains that the public key is unavailable and to try another profile, the connector rows and Copy public key are disabled and open nothing, and no stale key or prompt is shown.
18. [ ] **SET-018 — Help and support** — Open Help, open Report a bug and read it before tapping Open GitHub, then open Chat with support with and without an existing support DM. → **Expected:** Report a bug names the public issue form and states that no messages, media, contacts, profile details, keys, diagnostic logs or audit logs are attached; Open GitHub opens that form in the browser, and with no browser the screen stays and offers Retry and Cancel; existing support behaviour and external links are unchanged. Open Chat with support to review the contact before pressing Start chat: with an existing canonical DM, while relay loading is pending, with no receiving relays, after a read failure and Retry, and after returning from Open Relays. → **Support expected:** No contact, group or message opens merely by visiting. Start chat reopens the canonical existing DM even while configuration is unavailable; otherwise it presents the canonical support profile only after a configured native receiving relay list is read, leaving Message/KeyPackage/invitation handling in the normal production flow. Missing or unavailable relay configuration exposes recovery without synthetic connected counts. Relays Back returns Support; Support Back restores the Settings viewport. Account changes ignore stale reads. Sign-out blocks actions, and at 200% text/RTL the action and recovery rows remain reachable.
19. [ ] **SET-019 — About/build info** — Open Help → About and compare version/MarmotKit/build information with the APK record. → **Expected:** For this candidate, Settings and About show version `2026.9.21`; Android App info and the release manifest report version code `19`. MarmotKit/build values match the installed artifact, links/licenses shown open correctly, and no secret build path is exposed.
20. [ ] **SET-020 — Developer/diagnostics entry** — Open Settings → Developer Tools with the developer switch off, then turn it on. → **Expected:** The screen warns that these tools are for development and testing only; Key Packages is reachable either way; Debugging — streaming debug and Diagnostics — appears only while the switch is on; the App group reports the same version, build and MarmotKit revision as About; staging builds retain their badge here. About retains its seven-tap version unlock and shows its Developer destination only when enabled; Settings → Developer Tools remains available when disabled. With streaming debug enabled, show and hide a group-system row’s details; the summary stays visible and the details stay hidden when streaming debug is off.
21. [ ] **SET-021 — Diagnostics refresh/copy** — Open Developer Tools → Diagnostics. Check Events and the truthful Live/Idle state; use Actions → Health to refresh the relay snapshot, inspect every relay counter and runtime value, then close the sheet and navigate back. In a build with performance logging, opt in and out and inspect its remaining session time. While opted in, send text and media in a prepared agent chat, immediately return to Chats, and observe ordinary Android logcat for `I/WNPerf`; repeat with controlled send delays that cross 10 seconds and 60 seconds. Open uncached media once by tapping it and once through automatic loading, then repeat after the memory and disk caches are warm. On a designated disposable test identity only, use Send to self and confirm its result; while sending, both send actions must be disabled. Clear removes the displayed event log only. After a superseded group change in a prepared concurrent-edit scenario, verify the event shows the account label and generic group-event text without identifiers or payloads. Exercise existing copyable diagnostic detail rows and verify the full requested value is copied. → **Expected:** Existing refresh, native diagnostics, privacy, copy and recovery behavior remains intact; Health scrolls at large font sizes and all native values remain reachable. The Diagnostics page is the opt-in switch and status, while performance entries appear in the regular Android log as schema-2 `I/WNPerf` lines rather than under Stored Diagnostic Logs. Text/media-send lines include sanitized app and MarmotKit revisions, typed send stage, attempt and connectivity/relay counts where applicable; accepted-pending sends record durable acceptance, Android convergence, and timeline/chat-list settlement. Media sends distinguish upload, reused-upload, publication, and transport/projection phases. Attachment fetches distinguish explicit from automatic requests, memory/disk cache probes, native snapshot/demand/state changes, plaintext readiness and failure without serializing attachment identity or content. A send still pending at 10 or 60 seconds emits the matching checkpoint with its current closed stage, including after navigating away and replacing the conversation controller. Entries contain no message text, account/group/event/attachment identifiers, relay or Blossom URLs, filenames, byte content, exception text or stack traces. Opting out stops new entries, and an app-process restart still ends the explicitly time-bounded session.
22. [ ] **SET-022 — Donate/external links** — Open Donate, tap the large Donate button, verify the browser destination, then return. Repeat in light, dark and AMOLED themes and with large text. → **Expected:** The support message and one full-width Donate button are visible; the button opens `https://ipf.dev/donate/?utm_source=whitenoise_android&utm_medium=app&utm_campaign=donations`. There is no QR code, payment-method selector or address-copy control. Back preserves Settings.
23. [ ] **SET-023 — Settings mutation failure** — Disable networking before changing a server-backed setting, then restore and retry. → **Expected:** Busy state ends, old value is retained on failure, error is honest, and one retry produces one mutation.
24. [ ] **SET-024 — Back from modal pickers** — Open each settings sheet/dialog and dismiss with Back/outside tap without selecting. → **Expected:** The prior value remains and focus/scroll return to the invoking row.
25. [ ] **SET-025 — Persistence matrix** — Change one value on every settings detail, force-stop/relaunch, upgrade the APK, and switch accounts. → **Expected:** Every value persists at its documented device/account/conversation scope and does not leak across scopes.
26. [ ] **SET-026 — Donate browser failure and retry** — On a prepared test device/profile where web links cannot open, open Donate and tap Donate. Restore browser access and tap Donate again. Repeat with large text and a dark theme. → **Expected:** The app stays on Donate and shows a readable error with retry/back guidance. The Donate button remains available; a successful retry opens the same Android-attributed donation URL and clears the error. Back still returns to Settings. With TalkBack enabled, the failure is announced without moving focus.
27. [ ] **SET-027 — Recommended dictation app** — With Offline Speech to Text uninstalled, open Settings → Dictation in both Play and Zapstore builds and tap its recommendation. Install Offline Speech to Text, return to White Noise, and reopen Dictation settings. → **Expected:** The missing-app recommendation identifies Offline Speech to Text as the recommended private, on-device transcription app and opens its canonical Zapstore page in both builds. The recommendation disappears after the app is installed.
28. [ ] **SET-028 — Default disappearing messages for new chats** — For Alice, open Settings → Privacy & Security and set Default disappearing messages to one week. Relaunch, create a direct chat with a new contact, and create a named group with an image and description; override the named group to one hour before creating it. Switch to Bob, confirm the default is Off, create another group, then switch back to Alice. Also open an existing direct chat and retry opening a newly created group after forcing its first authoritative read to fail. → **Expected:** Alice's one-week choice survives relaunch and account switches without leaking to Bob. The new direct chat is founded with one week; the named group is founded once with its one-hour override, image, and description; Bob's group is founded with Off. Opening existing chats changes no policy. Recovery opens the same canonical group without another create or retention update. Existing groups remain unchanged, and their Details control can still change only that group's policy.
29. [ ] **DIC-003 — Dictation speech-service capability routing** — Install exactly one enabled speech provider with an exported Android recognition service (including Offline Speech on Android 17), leave the system voice-input service unset, and start dictation from the composer three times in a row, watching the status area and the system microphone indicator on the first tap. Cancel once while the status area still says Checking speech service…, then dictate synthetic text. Repeat with a provider build that cannot transcribe app-supplied audio, with a provider whose engine has no downloaded model, with a preinstalled recognizer, and once more after updating the provider. → **Expected:** The installed recognition service remains discoverable when the system selection is unset. Each tap opens exactly one capture surface, either White Noise's own dictation controls or that provider's own recognition screen, never both for the same tap and never the provider screen after capture has already started. The first tap may briefly show Checking speech service… with no microphone indicator and no recording, and later taps reuse a conclusive answer for the same installed provider; cancelled or inconclusive checks run again, and a provider update makes White Noise establish support again. Preinstalled recognizers use their existing in-app recognition path without an empty-audio probe. A provider whose engine simply cannot produce a result keeps White Noise's own controls and reports that failure with recovery instead of sending you to the provider's screen; with the device offline and again with an on-device provider that holds no model, the reported cause never claims the speech service could not connect unless the provider actually reported a network error. Cancelling during the check ends everything, leaves the prior draft unchanged, and starts no capture or transcript.
30. [ ] **DIC-004 — Dictation provider selection** — Install two speech providers with enabled, exported Android recognition services, leave Android's recognition-service setting unset, open Settings → Dictation → Speech provider, pick one, then dictate from the composer. Change the choice while a dictation session is active. → **Expected:** Both recognition services appear in the provider choices; disabled, private, test-only, or incorrectly permission-protected services are not offered. The row shows the exact saved engine; each tap uses the chosen provider with no silent fallback to another provider; the in-session change applies only to the next gesture.
31. [ ] **DIC-005 — Dictation provider removal and updates** — With a provider saved, uninstall or disable that provider, or update it to a new version, then start dictation. → **Expected:** The stale choice is cleared, dictation asks again or reports no provider, and it never silently substitutes another service, Activity, or keyboard.
32. [ ] **DIC-006 — Dictation provider chooser rendering** — Open the chooser with providers in each capability state, at 200% font and in RTL, with TalkBack, and with no provider installed. → **Expected:** App/engine names and capability labels (Works inside White Noise, Opens provider window, Keyboard only, In-app support not verified) stay truthful, wrapped, and scrollable; the empty state explains the situation without claiming compatibility.
33. [ ] **TTS-003 — Unknown-engine trust warning** — Select an installed TTS engine that White Noise classifies as unknown, cancel the first warning, select it again and proceed, then leave and reselect the same engine. → **Expected:** Cancel keeps the previous engine; Proceed selects the unknown engine only after the warning; the acknowledgment is remembered for that exact engine so the warning does not repeat, while another unknown engine still warns.

### Notifications, background behavior, and app lock

1. [ ] **NTF-001 — Notification permission grant** — On Android 13+ deny initial permission, open Notifications and use Allow notifications; after denial use Notifications are off to open Android settings. Grant permission and return. Repeat with local notification rendering disabled for the account through a controlled native-settings fixture before granting permission. → **Expected:** Denial gives feedback and disables delivery-mode changes without erasing the settled mode. Grant restores the single mode selector without enabling an account's disabled notification rendering; only an explicit delivery-mode selection may enable it. Device status reports the live Android permission.
2. [ ] **NTF-002 — Local and push delivery** — With a disposable account, select Local notifications and receive a DM while backgrounded, then select Push notifications and repeat after the persistent service has stopped. Revoke and restore notification permission in each mode. → **Expected:** Exactly one mode is selected. Local uses the acknowledged persistent connection; Push uses generic one-shot wakes after registration confirmation. Permission loss pauses visible notifications while retaining the transport choice, and restoration resumes the same choice.
3. [ ] **NTF-003 — Delivery cutover** — Alternate Local and Push quickly, including a rejected foreground-service start, failed registration, process restart, account switch, and an A-to-B-to-A account return. → **Expected:** The replacement path is confirmed before the prior path is disabled, the latest intent wins, no stale owner mutates the new account/runtime, and failures leave an existing delivery obligation available for retry. There is no separate Keep connected control.
4. [ ] **NTF-004 — Push availability** — Inspect Notifications on an available build and on controlled builds/devices missing each push prerequisite; restore capability after settling on Local. Open the battery-policy row on a normal device and on a controlled profile/build that rejects the dedicated battery surface and app-details fallback. Repeat with large text and RTL. → **Expected:** Push is omitted from the selector whenever unavailable, Local remains the sole selected choice, and the specific cause appears outside the selector. Capability restoration makes Push available but never switches modes automatically. Battery policy opens the dedicated settings surface or app-details fallback; if Android rejects both, White Noise reports that settings could not be opened without crashing.
5. [ ] **NTF-005 — Notification channels** — Open each category row: Direct messages, Group messages, Mentions, Reactions, Invites, Group membership, Agent activity, and App updates. → **Expected:** Android opens the matching channel settings, not the wrong channel or whole app page.
6. [ ] **NTF-006 — DM/group first draw** — With Alice's profile already known on this device, background White Noise and send synthetic DM/group messages that cover styled Markdown with a mention, a captioned and captionless attachment, and an ordinary text message. Run the first message after a fresh app-process start without opening a conversation or prewarming Markdown, then repeat warm. Observe the first displayed card and any later silent update. On an unlocked device with heads-up notifications enabled, repeat with cached avatars and a new synthetic sender/group whose avatar loads after the initial banner; compare recordings on the same device/settings without tapping or swiping. → **Expected:** The first card contains locally resolved sender, recipient/group context, rendered text, and media kind when resolution fits the absolute 100 ms content-stage limit. Already-decoded local imagery appears immediately; otherwise the intentional monogram stays stable for that card, with no cosmetic avatar-only rewrite. Remote images may prepare a future card but never delay first publication. Timeout/failure still posts a prompt privacy-correct nonempty fallback; at most one matching text correction preserves grouping/channel, accumulated history and dismissal/lock/account ownership without a second sound/vibration or premature banner collapse. Android controls natural banner lifetime and the card remains in the shade afterward. Then put the device in airplane mode, have Alice send eight messages and one mention over two minutes, and go back online: every card appears with its unread content, the whole backlog sounds or vibrates at most once per signed-in account, and a fresh message arriving once catch-up has settled rings normally; a burst of fresh messages rings for its first card only, while a fresh mention rings even inside a burst.
7. [ ] **NTF-007 — Mention/reaction routing** — Mention Alice and react to Alice's message while backgrounded. → **Expected:** Events use Mentions/Reactions channels and do not also create duplicate generic notifications. Send a burst of ten reactions and mentions within two seconds to one group: every card ends with its final content and no card the app dismissed stays on screen, because card writes are paced under Android's five-per-second limit and dismissal only cancels cards that are shown or that the app has just written.
8. [ ] **NTF-008 — Invite/group-system routing** — Send an invite, then use disposable groups to add/remove a member, promote/demote an admin, rename the group, change its avatar, and enable/disable disappearing messages; repeat actor/target variants involving Alice. Remove Alice from a named group and an unnamed group, and inspect collapsed/expanded cards and locked-screen privacy with normal and large text. → **Expected:** Distinct notifications use the correct channels, localized actor/target/group copy matches each change without an unrelated identity, and every notification opens the matching account/group state. Removal uses one concise sentence naming the group (or “a group”), without a second actor/removal sentence or empty expandable body. Admin-change notifications retain their useful detail, and redacted cards reveal no group or actor names.
9. [ ] **NTF-009 — Notification tap** — Prepare a group chat containing exactly Alice and Carol, keep Bob's account active, and tap a synthetic Alice notification for that group. Leave the revealed conversation mounted, background the app, receive another synthetic notification, and tap it to return to the same conversation. Repeat with a disposable cold fixture that has no cached Alice member list, including a tap while the previous account screen is still settling. Automated companion coverage injects a deterministic roster-read failure before returning members, then lets the same reader succeed and taps Retry; ordinary offline mode alone does not prove that branch. It also injects the engine's exact terminal eviction response during route startup. While the recovered route remains open, have Carol add Bob and remove him again. Also open a fully loaded empty conversation from the chat list without a notification tap, keep it foregrounded, and have its peer send the first message. → **Expected:** The app binds Alice and the exact group before exposing its transcript; Bob's member list or messages never flash. The repeated tap owns its fresh request without recreating the already-mounted conversation controller. While the transcript is hidden or a roster Retry is showing, fresh target messages remain notification-eligible; after the authoritative transcript is revealed, its visible route suppresses them. An unknown cold member list shows loading followed by a recoverable error, rather than guessed sender chrome, and Retry recovers on the same route. Terminal eviction stops loading, shows the existing removed-member notice and no editable composer. The two-member compact chrome changes to ordinary group sender gutters at three members and returns at two, while the chat remains a group and its message/scroll state and unrelated notifications remain intact; only the tapped target clears appropriately. An ordinary settled empty conversation suppresses its foreground peer notification, and the first incoming row still receives normal initial positioning. A notification-routed group reveals its transcript once its roster is known, and that read runs alongside the conversation's timeline and group-state opens rather than after them, so a tap into a group whose roster is cold is no slower than the slowest single read. Tap a notification for a conversation on the already-active account while that conversation is momentarily absent from the visible chat list — for example straight after a cold start, before the list has caught up: the tap opens the conversation once the exact per-group read finds it, and never reports that the conversation is no longer available on the strength of the list not showing it yet.
10. [ ] **NTF-010 — Notification actions/dismissal** — Exercise every displayed notification action and swipe-dismiss, including while app is foreground. Include the background-and-return repeat notification from NTF-009. Dismiss a cold-avatar card before its avatar finishes loading, then wait for that load to complete. → **Expected:** Each action affects the intended account/event once; dismissal has no chat-side effect and delayed avatar enrichment does not resurrect the dismissed card or alert again; the replacement tap owns its own visibility transition, and only the actually visible foreground conversation suppresses redundant alerts.
11. [ ] **NTF-011 — Locked-screen privacy** — Receive synthetic events with the device locked under each Android content-visibility setting. → **Expected:** White Noise honors OS/channel privacy; no secret key, full private content, or wrong-account data is revealed.
12. [ ] **NTF-012 — Per-chat override precedence** — Combine a global channel setting with mute/notify overrides and send ordinary, mention, and reaction events. → **Expected:** Per-chat choices override defaults as labeled and mute expiry restores expected delivery. Then open one member's profile from inside a group and use **Mute in this group**, with a second local account signed in and a member of the same group. → **Expected:** That member's ordinary messages, mentions and reactions stop notifying in that group for the selected account only; the same person still notifies in another shared group, the other local account is unaffected, and every other member of the muted group keeps notifying. Membership and admin notifications — removal, invites, promotion and demotion — arrive as before. The row reads **Unmute in this group** afterwards, survives force-stop and relaunch, and reverses on a second tap. The row is absent on the tester's own profile, on a direct message, and on a profile opened away from a group; muting a member never blocks them and never mutes the whole conversation.
13. [ ] **NTF-013 — App update notification** — On a build/feed with a newer controlled version, allow the scheduled/foreground check. → **Expected:** One App updates channel notification appears and opens the update action for the installed variant.
14. [ ] **NTF-014 — Reboot persistence** — Enable required background notification settings, reboot, unlock, and send a synthetic message. → **Expected:** Settings persist and supported background delivery resumes without opening White Noise first, or the limitation is stated honestly.
15. [ ] **NTF-015 — Stale notification cleanup** — Sign out/wipe an account with outstanding notifications and tap any stale system entry. → **Expected:** Entries are removed or open a safe fallback; wiped account content cannot rehydrate.
16. [ ] **NTF-016 — Inline reply** — Reply with synthetic text from a DM notification while the app is backgrounded, first offline and then online. → **Expected:** Android shows sending/retry state without exposing another account; one reply is delivered when connectivity permits and appears once in the intended conversation.
17. [ ] **NTF-017 — Quick reaction choices** — Use every quick-reaction chip exposed by a message notification, repeat one chip rapidly, then open the conversation. → **Expected:** Exactly the selected reaction is applied once to the intended message/account and notification state updates without a duplicate generic alert.
18. [ ] **NTF-018 — Mark read from notification** — Use Mark read on DM and group notifications, including when another account is foregrounded. → **Expected:** Only the originating account/chat is marked read, its notification and unread badge clear, and foreground account state remains unchanged.
19. [ ] **NTF-019 — Push recovery dispatch** — On a disposable test identity, deliver high, normal, downgraded and deleted-backlog callbacks; reject a high-priority service start and repeat with the persisted internal Keep connected preference on/off and notifications denied. Configure that preference through the controlled service fixture because it is no longer a Notifications-screen control. Include API 30 and a current API device. → **Expected:** Every accepted wake is durably recorded; normal/deleted callbacks schedule connected-network work, eligible high priority keeps the short service route, rejection falls back, and API 30 uses regular work. Sync does not depend on whether a notification can be shown; user settings remain unchanged.
20. [ ] **NTF-020 — Burst, restart and retry recovery** — Hold catch-up, send 100 more callbacks, then release. Repeat a callback at worker completion, kill/reopen the disposable process between marker/enqueue, interrupt network, and force four native failures. Send a later wake after the four-minute exhaustion cooldown; repeat across account switching and app unlock. → **Expected:** One native catch-up runs at a time with at most one pending successor; newest generation remains pending until fresh success. Work survives restart, failures back off from 30 seconds, burst pushes do not reset the budget, and exhausted work waits for an external trigger. Wipe fences prevent stale work, screen lock retains notification redaction, and quiet success stops retries.
21. [ ] **NTF-021 — Push battery and latency qualification** — Alternate five baseline/candidate trials for foreground keyboard-open chat, screen-off delivery, Doze, airplane recovery, network changes and both connection modes. Record native attempts, starts, CPU, bytes, wake-lock duration and median/tail delivery latency. Run three paired idle sessions of at least two hours on a physical test device with no pending wake. → **Expected:** No new periodic tasks or idle recovery wakeups, no overlapping native work, no extra manual worker wake lock, and no unexplained latency regression. Record exact artifacts and energy variability; emulator timing is not battery evidence. Compare the original affected release separately before claiming the open-chat symptom is fixed.
22. [ ] **NTF-022 — Delivery mode restoration and legacy convergence** — Prepare every legacy local/native/background boolean combination in a disposable fixture, then cold-start, foreground before bootstrap finishes, restore the same account, switch accounts and rapidly select both modes. Include registration success/failure; a persistent-service acknowledgement, rejection, supersession and teardown; cancellation after the durable preference commit, after each account native-disable commit, during registration cleanup, and after service stop begins; and a multi-account partial failure. Switch to an account whose local rendering preference is off under both settled device modes, and switch into a rendering-enabled/native-enabled account while the retained device mode is Local. Revoke notification permission before foreground and activation reconciliation while push capability is unavailable, then grant it again. → **Expected:** Existing native settings and the global persistent-runtime preference project exactly one mode without a second Android-owned mode record. Dual-on converges to Local, unavailable push migrates only after persistent readiness, registration-confirmed Push stops persistent delivery, and stale generations never disable the current path. Once either transport mutation commits, cancellation/failure leaves an acknowledged path active; a stopped Local path is re-established before native rollback. Cold, foreground, and account activation preserve each account's explicit rendering opt-out while converging the device transport; explicit delivery-mode selection can enable rendering. A real account switch reconciles every signed-in account even when active-account rendering already matches. Permission loss retains the settled mode and required registration cleanup but performs no registration, mode mutation or persistent-service start; regrant preserves the rendering preference during asynchronous convergence under the captured account/runtime/switch owner. A superseding activation that needs no cutover releases the busy selector.
23. [ ] **NTF-023 — Notification device policy** — Open Notifications under allowed/blocked permission and optimized/unrestricted/restricted battery states where the OS exposes them. Use the battery row, change policy, and return. Repeat in light/dark/AMOLED, RTL and 200% text with TalkBack. → **Expected:** Status refreshes on lifecycle return without polling, the action opens app battery settings or app details, no exemption request appears, all rows remain reachable and announced, and copy distinguishes blocked alerts under denied notification permission from possible background delivery delays under battery policy.
24. [ ] **NTF-024 — Push diagnostic correlation and export** — Explicitly enable the bounded performance session, deliver two overlapping callbacks with different delivered/original priorities, coalesce their recovery, and exercise posted, suppressed, failed and missing-post outcomes. Copy logs from Diagnostics before and after session expiry. → **Expected:** Every callback has a distinct anonymous recovery token and ordered phases through catch-up/projection/post where observed; shared phases carry bounded counts, missing outcomes are marked incomplete, the session is capped at 30 minutes/256 events, and copied text contains no payload, token, account, group, sender, message content or exception detail. No collection timer or network upload is introduced.


### Device privacy and audit controls

1. [ ] **SEC-001 — App-lock prerequisite** — On a device without secure credential open Settings → Privacy & Security and inspect Require device authentication and the Open Android security settings row, then configure a PIN/biometric and return. → **Expected:** Toggle is initially disabled with guidance and becomes available after credential refresh.
2. [ ] **SEC-002 — Enable/disable app lock** — Enable Require device authentication, background beyond its Auto-lock delay, resume and authenticate; then disable. → **Expected:** Chat UI is covered before it can be read, success restores the prior route, cancellation keeps it locked, and disabling prevents future lock screens.
3. [ ] **SEC-003 — App-lock delays** — Test every Auto-lock delay in its dialog with boundary-short and boundary-long background intervals. → **Expected:** Lock appears only at/after the selected threshold and cannot be bypassed through rotation, notification tap, Recents, or account switch.
4. [ ] **SEC-004 — App-lock failure and PIN lifecycle** — Fail/cancel system authentication repeatedly, then complete ten consecutive device-PIN unlock cycles on Android 17/API 37 and at least one supported older API. During separate cycles, rotate/recreate the Activity while the PIN prompt is open, background and resume it, enable Don't keep activities so a fresh Activity returns without a retained prompt, and enter through a notification-routed conversation. → **Expected:** No content flashes. A non-terminal failed credential leaves the system prompt active. Cancellation is terminal: the opaque cover stays visible and the error state exposes Retry. Lockout and unavailable credentials are terminal, keep the cover opaque, and expose their specific error state plus Retry when Android permits a new attempt. Every successful PIN returns to the same destination exactly once without closing White Noise, restarting its task, or immediately prompting again. Recreation reconnects an actually retained system prompt instead of launching a second prompt. When Android did not retain the prompt, the fresh Activity keeps the opaque cover and launches one new prompt without requiring a force-stop. A stale callback, cancellation, lockout, or unavailable credential never clears the app-owned lock cover. Verify the cover uses the centered lock glyph and Unlock action, stays opaque while Checking app protection is shown, announces errors, and remains scrollable at 200% font/RTL and in short landscape windows. Inspect Light, Dark and AMOLED including the Unlock outline. Retry requests a real Android prompt only after the prior attempt reaches a terminal state; repeated taps cannot replace an active credential prompt. Cancel/Back must never expose chats. Confirm screenshots/Recents remain protected while locked and that another secure surface keeps protection after successful authentication. On a debug or performance-selector build, capture `WNAppUnlock` lifecycle/session markers if any cycle closes, duplicates a prompt, or loses its destination; release builds must not emit them, and the markers must contain no credential material, authentication modality, or raw platform error code.
5. [ ] **SEC-005 — Incognito keyboard** — Toggle Incognito keyboard and inspect supported keyboard behavior in composer/search/profile fields. → **Expected:** Incognito request applies to sensitive text entry when enabled and normal behavior returns when disabled; unsupported keyboards do not produce a false claim.
6. [ ] **SEC-006 — Chat screenshots** — Toggle Screen security and attempt screenshot/screen recording in Chats, a conversation, keys, and non-chat settings. → **Expected:** Protected surfaces are blocked exactly as labeled, allowed surfaces work when enabled, and setting persists.
7. [ ] **SEC-007 — Usage and telemetry opt-in/out** — Upgrade a backed-up disposable installation that granted the old relay-only toggle. Review Help Improve White Noise and Share usage and telemetry, grant sharing, relaunch, revoke from Privacy & Security → Diagnostics & Improvements (the row's value reads Off, Usage, Logs or On), and relaunch again. → **Expected:** Expanded scope requires renewed MDK consent even when the old decision was GRANTED. Decline/grant persists, legacy export intervals are preserved, and revocation invalidates old observations. Logs remain independently controlled. No identifiers, message contents, queries or keys are event properties. The first-install consent sheet closes the moment its X or a downward swipe is used, without waiting for the declined choices to be written, and a second tap changes nothing; picking either option leaves the sheet in place rather than closing and reopening it.
8. [ ] **SEC-008 — Technical log sharing** — Enable Share group diagnostic logs from Diagnostics & Improvements and from Help Improve White Noise. Cancel once, then confirm; exercise activity, revoke and relaunch. Upgrade a disposable installation with legacy audit logging enabled. → **Expected:** Both entry points disclose automatic Goggles uploads, recipient, sensitive metadata, existing-file upload and revocation limits before enabling. Cancel sends nothing. Legacy logging is disabled before background/native startup and requires a fresh choice at unobstructed Chats, separately from usage consent. Decline stays off across restart. Failed renewal cannot start an uploading runtime. A failed settings save shows an error, never a success notice; retry the choice and relaunch to verify the saved state. After consent, v4 logs use the system model and no keys/message content; legacy files remain ineligible. Revocation removes upload authorization and stops recording; no new upload batch starts. A batch already in progress may finish uploading its files, as disclosed; stored files are not deleted. The revised disclosure stays direct and readable while still naming every included and excluded field, Goggles and the Internet Privacy Foundation, the sensitivity and reliability purpose, its separation from usage and telemetry, existing-log upload, in-flight-upload behavior, stored-data behavior and Cancel/off behavior.
9. [ ] **SEC-009 — Export diagnostic logs** — Tap Export Diagnostic Logs under Stored Diagnostic Logs, cancel consent, then confirm and read the destination choice. Take **Share** once into a safe local destination, then repeat and take **Save to device**, cancelling the system document picker once before completing it. Repeat both with several stored logs and with none. → **Expected:** Cancel creates/shares nothing; repeat in a non-English language, at 200% font and in RTL, and verify the row, both dialog titles and bodies, and the Save/Share buttons are translated and legible; consent explains that logs contain technical identifiers and activity details and should only be shared with a trusted recipient, without contradicting the diagnostic-data exclusions above. One export produces exactly one `.zip` whose name carries no account, group or device detail, containing every stored log once under plain relative names — no folders, absolute paths or `..` entries. Share attaches that single archive rather than separate files; Save writes it through the picker with no storage-permission prompt, reports success, and a cancelled picker does nothing at all. Extracted entries are readable, bounded, and omit secrets and message content.
10. [ ] **SEC-010 — Clear diagnostic logs** — Under Stored Diagnostic Logs, verify On This Device and tap Clear Diagnostic Logs. Read Clear diagnostic logs?, cancel, export to prove preservation, then confirm with Clear Logs and export again. → **Expected:** The description below the button says clearing affects only this device and leaves uploaded copies and the sharing setting unchanged. Confirmation states that local logs for all profiles are permanently deleted and that sharing settings stay unchanged. With TalkBack enabled, focusing Clear Diagnostic Logs includes the local-only clearing explanation; the visual footnote is not announced again as a separate stop. Clear success, empty and failure notices use diagnostic-log terminology. Cancel preserves logs; Clear Logs removes existing logs without changing logging preferences or deleting app data.
11. [ ] **SEC-011 — Product configuration and ingestion** — Supply separate staging/production Aptabase variables from `docs/product-analytics.md`, run the resolved configuration gates, and exercise screens, notification permission/entry, share/profile entry and the 26 bridge timings with synthetic data. Repeat with absent config and previews. → **Expected:** The optional product configuration checks reject missing or malformed values without printing credentials; missing Aptabase values do not block an OTLP-only release. Development/previews remain unconfigured. Explicitly opted-in configured staging builds produce persisted Android/staging events in the correct Aptabase application; HTTP acceptance alone is insufficient. Verify with the operator that deployed servers enforce the disclosed 30-day group-log, 90-day telemetry and 180-day product-analytics retention. The optional configuration check requires endpoint, app key and native operator metadata; it does not accept an unlocalized retention override or prove server enforcement.
12. [ ] **SEC-012 — First-launch sharing choice** — Open a disposable fresh installation; verify Welcome, sign-in/signup, and account setup have no sharing sheet. Complete setup and reach Chats; inspect the bottom sheet and both default-off choices, leave them off and close the sheet, then relaunch. Repeat granting usage while leaving logs off; inspect light/dark, RTL and large text. → **Expected:** Closing saves decline by default; the full disclosure, independent log switch and close action remain readable/reachable. Both switches are grouped above the full disclosure. The title is Help Improve White Noise and the introduction reads “Help us understand how White Noise is used and make messaging more reliable.” Compare both toggle labels, their all-profiles descriptions and all five disclosure paragraphs with Diagnostics & Improvements: they match in every supported language. The revised wording stays concise and reads consistently across the first-launch sheet and Diagnostics & Improvements. Verify temporary session identifiers, resettable installation identifier, no account/group identifiers for usage or telemetry, hashed member references in group logs, excluded contents/names/keys, possible existing-log uploads, retention of 30/90/180 days, and the in-progress upload and stored-data limits when disabling. Scroll to the final paragraph at large text sizes. Tap a row label or switch: usage responds immediately; enabling group diagnostic logs first opens the explicit automatic-upload confirmation titled Share group diagnostic logs with White Noise?. The confirmation body and renewal notices use the same group diagnostic log terminology, and refer to the separate usage and telemetry choice. After confirmation its selected state responds before persistence finishes. During either save, the selected switch keeps its color, repeated taps on that choice are blocked, both switches stay in place, disclosure text does not rewrap or flash loading/exporter states, and Close keeps its position. A resolved receipt prevents repeat prompts and neither switch changes the other. An existing signed-in account with a pending receipt sees the sheet at Chats; opening directly into a conversation, profile, share picker, or another sheet defers it until unobstructed Chats returns. Notification permission waits until the sharing choice is completed.
13. [ ] **SEC-013 — Sharing failures and stale observations** — Inject a receipt read/write failure, retry, then exercise revocation/regrant, account switching, runtime replacement and background/resume while operations are pending. → **Expected:** Failed decisions remain visible with Retry and never show a stale successful grant. No operation started before permission or under an obsolete context is replayed. Exporter status labels usage and telemetry separately from group diagnostic logs, and distinguishes off, permission needed, unconfigured, unsupported, rejected and ready; ready does not claim server ingestion. A persistence failure says White Noise could not save the sharing choice and offers a retry, without implying that consent succeeded.
14. [ ] **SEC-014 — Blocked users list** — Block two people from their profiles, open Settings → Privacy & security → Blocked users, unblock one, then interrupt relay access and reopen the list. → **Expected:** The list names both accounts and the Privacy row shows their count; unblocking removes only that row after confirmation; while the block list cannot be read the screen says so instead of claiming nobody is blocked.

### Relays, updates, and system intents

1. [ ] **SYS-001 — Relay list states** — Open Relays for a complete, incomplete, and loading/unavailable projection. → **Expected:** Where I post and Where I receive read Published, Missing or Status unavailable with the matching explainer; Publish missing lists appears only for a gap, Refresh status otherwise, and a progress row or Retry while an operation runs or after it fails; every relay is listed once with the lists it serves.
2. [ ] **SYS-002 — Relay URL validation** — Enter empty, malformed, `http://`, duplicate, unsafe/private, retired, White Noise-managed, and external public `wss://` values. → **Expected:** Empty, malformed, `http://`, and duplicate values disable Add locally; acceptable nonduplicate White Noise and external URLs enable it. MarmotKit rejects structurally valid unsafe or retired endpoints before publication.
3. [ ] **SYS-003 — Add/remove relay** — Import an account with multiple valid external relays, append a White Noise-managed relay, then remove one relay at a time. Repeat with an unreachable external hostname already in the imported list. → **Expected:** Submitting Add relay keeps the sheet open, disables the action, and replaces its label with localized publication progress without crashing; each edit preserves every other MarmotKit-allowed relay, removal affects only the selected entry, bootstrap relays are used only when no relay remains, singular progress appears, published lists refresh, and failure retains authoritative state.
4. [ ] **SYS-004 — Share into White Noise** — From another app share plain text, one image, multiple media/files, and unsupported content to White Noise. → **Expected:** The full-screen chat picker previews the payload accurately, lets the tester choose the sending account/chat, and does not send before confirmation.
5. [ ] **SYS-005 — Share picker navigation** — Search/select a target, switch sending account, press predictive/system Back, close, and repeat send. → **Expected:** Back/close cancels without sending; account switch refreshes eligible chats; confirmed share appears once in the selected chat.
6. [ ] **SYS-006 — Deep profile links** — Open a supported flavor-specific White Noise profile URI and `marmot://profile/<npub>` from another app in cold and warm states, then try a malformed, event, and invite URI. → **Expected:** Supported profile URIs open the correct in-app profile once; malformed and unsupported event/invite forms do not route to an unrelated screen or expose stale account data.
7. [ ] **SYS-007 — Clipboard lifecycle** — Copy ordinary text, `npub`, and a disposable secret-key value through their intended controls; background/finish flows. → **Expected:** Copy confirmations identify what was copied, sensitive clipboard content is cleared where promised, and another account is never substituted.
8. [ ] **SYS-008 — Zapstore self-update** — On a Zapstore build with a controlled newer release, open the compact Chats update icon immediately before Search. It opens Settings without resolving/downloading. Open App updates and exercise release resolution, Cancel, confirmation, download, trusted verification, permission review/return, and Android installer handoff. Check normal and three-or-more-release important status, plus Search and selection mode. → **Expected:** The rounded download glyph sits on the teal nine-sided background in both the header and Settings update row. Settings shows Check for updates before a result, Up to date for a current version, or Version <actual version> is available on Zapstore. after a real newer release is found. A failed check shows Tap to retry; another failed retry must not start a cached update. The icon uses real newer-version data and is absent during Search/selection or when no newer release is known; no large Chats banner remains. Download completion alone never enables Install. Only a matching trusted APK reaches Ready; hash/package/signature rejection cannot install. Cancel/retry use the existing flow and storage cleanup, permission return refreshes eligibility, and installer handoff never claims the version installed. Verifying update begins at the checksum comparison and continues through package/signature trust checks; it must never offer Install. Cancel at either verification boundary, including a delayed native return, and retry the same version repeatedly while the first check remains pending: cancellation keeps Idle, removes untrusted files, and old cleanup cannot delete the replacement APK. A very fast real verification may finish within one frame; do not add artificial delays to force visibility.
9. [ ] **SYS-009 — Play update ownership** — Open Chats and Settings on a Play build, including a stale/newer update record left in test storage. → **Expected:** No off-store update emblem, Settings update section, self-update dialog, Zapstore redirect, APK download or install-package action is exposed. The distributing store retains update ownership.
10. [ ] **SYS-010 — Current, failed and dismissed updates** — Check current data and a failed check while retaining the last known release. For an ordinary newer release, open update review and choose Dismiss update while resolving, confirming, or after a resolution failure; retry through Settings. Discover another version and repeat with an important release. → **Expected:** Current data shows no Chats availability icon; check errors remain recoverable and version facts stay intact. Dismiss update retains the existing per-version notification-reminder policy and cancels the current review; it does not schedule a new reminder or hide the compact availability icon. Important updates have no Dismiss update action. A stale dialog cannot dismiss a different newly discovered version, and no UI action claims installation success.
11. [ ] **SYS-011 — Android Direct Share shortcut** — From another app choose a White Noise conversation shortcut, first while its account is active and then while a different account is active; also invoke a stale shortcut. → **Expected:** A valid shortcut stages the payload for the exact account/chat and still requires the displayed send confirmation; a stale or wrong-account shortcut falls back to safe target selection without leaking chat details.

### Accessibility, layout, and resilience sweep

1. [ ] **AXY-001 — TalkBack traversal** — With TalkBack, traverse onboarding, Chats, conversation, attachment pane, profile, Settings, and all dialogs touched above. → **Expected:** Interactive controls have unique meaningful names/roles/states in visual order; decorative icons are not announced.
2. [ ] **AXY-002 — TalkBack actions** — Complete sign-in field validation, send, react, select, toggle, dismiss, and Back using TalkBack only. → **Expected:** State changes are announced, custom actions are operable, focus returns sensibly, and no gesture-only control blocks completion.
3. [ ] **AXY-003 — Large text/display** — Set Android font/display to largest supported values and White Noise font to Extra large; repeat core onboarding/send/settings flows. → **Expected:** Text wraps, lists scroll, primary/destructive actions remain reachable, and no content overlaps system bars.
4. [ ] **AXY-004 — Landscape/small height** — Rotate every major screen, open keyboard and modal on a short-height window. → **Expected:** Content scrolls or resizes, dialogs stay actionable, and composer/buttons are not hidden.
5. [ ] **AXY-005 — Tablet/foldable/resizable** — Resize across narrow/wide postures where available. → **Expected:** Content uses readable width, navigation does not duplicate, and state/selection/draft survive posture changes.
6. [ ] **AXY-006 — RTL** — Select an RTL system locale, relaunch, and traverse Chats/conversation/settings/media viewer. → **Expected:** Layout/directional icons mirror appropriately, text alignment is readable, and Back/media gestures remain correct.
7. [ ] **AXY-007 — Color/contrast** — Inspect all themes, custom colors, error/success/disabled states, and Android high-contrast/color-correction settings. → **Expected:** Meaning is not color-only and text/icons meet practical readability without disappearing.
8. [ ] **AXY-008 — Reduced animation** — Disable Android animations and repeat navigation, sheets, image viewer, account switch, and startup. → **Expected:** Final states appear without timing dependence, invisible controls, or stuck transition layers.
9. [ ] **AXY-009 — Touch targets and gestures** — Use one-handed taps near screen edges and system gesture navigation for top bars, FABs, reactions, menus, and composer. → **Expected:** Controls have reliable targets and do not conflict with Back/home gestures.
10. [ ] **AXY-010 — Keyboard-only/hardware keyboard** — Navigate fields/dialogs with Tab/Shift-Tab/Enter/Escape and test configured Enter behavior. → **Expected:** Focus is visible and ordered, Escape/Back dismisses the top layer, and Enter follows the selected setting.
11. [ ] **AXY-011 — Rapid repeat protection** — Rapidly tap create/send/retry/delete/invite/update/settings mutation actions. → **Expected:** At most one operation is active, controls show busy/disable appropriately, and no duplicate message/group/account/file is created.
12. [ ] **AXY-012 — Low storage/interrupted process** — With a safe low-storage condition or forced process death, attempt media save/export/update and reopen. → **Expected:** Failure is explicit, partial files are not presented as complete, and the app recovers without data corruption.

### Completion and reporting

1. [ ] **END-001 — Untested-point audit** — Search this file/run record for unchecked points and `N/A`. → **Expected:** Every point is Pass, Fail, or justified `N/A`; every failure names its stable ID.
2. [ ] **END-002 — Cross-device consistency** — Compare Alice/Bob/Carol membership, latest messages, edits, deletions, reactions, notification settings, and disappearing state after final synchronization. → **Expected:** All devices converge or every divergence is reported with point ID and timing.
3. [ ] **END-003 — Cleanup** — Remove exported files, revoke temporary permissions, clear sensitive clipboard, delete synthetic contacts/media, and wipe disposable identities as authorized. → **Expected:** Test artifacts no longer appear in storage, notifications, Recents, clipboard, or account selector.
4. [ ] **END-004 — Release verdict** — Attach the completed checklist and summarize failures by severity and point ID against the recorded APK SHA. → **Expected:** The report states one unambiguous PASS or FAIL for that exact artifact and never transfers results to a different build.

## Report a failure

For each failed ID, record the exact APK SHA-256 and commit, device/Android/build variant, active account and conversation type, numbered reproduction actions, actual result, expected result, occurrence time, reproducibility, severity, and privacy-safe evidence. Never include an `nsec`, recovery secret, private message text, audit-log contents, precise personal location, or real contact.

## Maintainer coverage map

This checklist was derived from the current navigation, UI, resources, manifest declarations, tests, and repository documentation, including:

- `app/src/main/java/dev/ipf/whitenoise/android/ui/WhiteNoiseApp.kt`, `ui/navigation/`, `ui/onboarding/`, `ui/chats/`, `ui/conversation/`, `ui/group/`, `ui/profile/`, `ui/qr/`, `ui/search/`, `ui/settings/`, and `ui/share/`
- `app/src/main/java/dev/ipf/whitenoise/android/state/`, `notifications/`, `updates/`, and `MainActivity.kt`
- `app/src/main/AndroidManifest.xml` and `app/src/main/res/values/strings.xml` plus locale resources
- `app/src/test/`, `app/src/androidTest/`, and build-flavor tests under `app/src/testPlay/` and `app/src/testZapstore/`
- `README.md`, `docs/`, `.github/workflows/android-ci.yml`, and release/verification scripts

A feature is in scope when a release build can present it to a user, including loading, empty, disabled, permission-denied, offline, error, retry, destructive-confirmation, account-switch, background, and restored states. A visible Coming soon control is tested as a disabled/explicit state; a source-only feature with no release-build user path is not represented as working UI.

The shared settings components in `ui/settings/SettingsComponents.kt`, `ui/settings/SettingsRowDefaults.kt` and `ui/common/WhiteNoiseDialogs.kt` have their first release-build user path on the Appearance screen and are covered by `SET-001`–`SET-008`. `ui/common/WhiteNoiseTextField.kt` and the shared `SettingsSwitch` row have no release-build user path yet; they are listed in `docs/manual-release-testing-surfaces.json` as explicit coverage exceptions and are pinned by `WhiteNoiseTextFieldBehaviorTest`, `WhiteNoiseTextFieldScreenshotTest`, `SettingsComponentsSemanticsTest` and `SettingsComponentsScreenshotTest`. When a screen adopts them, replace those exceptions with permanent owning-prefix IDs in the same pull request.

### Maintenance contract

- Any pull request that adds, removes, renames, or changes user-visible behavior or state must update the affected point(s) in this guide in the same pull request. This includes permissions, intents, navigation, settings, error/retry copy, build-flavor differences, background work, privacy behavior, and accessibility behavior.
- Keep existing IDs forever. Edit a point in place when its feature changes; append a new ID within the owning prefix for a new independently reportable behavior; mark removed behavior as retired in the pull request rather than reassigning its ID.
- Every checklist point must remain an ordered Markdown checklist item with exactly one stable `<AREA>-<NNN>` ID, an action instruction a novice can execute, and one observable `**Expected:**` result after the `→` separator.
- Before opening or updating a pull request, run `python3 scripts/check_manual_test_guide.py`, `python3 -m unittest scripts/test_check_manual_test_guide.py`, and the repository's required Hermes fast gate. Review the scope map whenever navigation, resources, manifest components/permissions, tests, or release documentation changes.

## Retired IDs

Never delete an old ID. Move it here when its user-visible behavior is removed.

| ID | Retired in | Reason | Superseded by |
|---|---|---|---|
| MED-002 | #2751 | The recent-media permission flow was removed in favor of Android's system photo picker. | MED-001 |
