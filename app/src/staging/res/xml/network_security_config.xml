<?xml version="1.0" encoding="utf-8"?>
<!-- Staging keeps loopback test-relay support without shipping it in production.
     10.0.2.2 (the emulator's host alias) is deliberately absent: staging builds are
     release-signed and run on real hardware, where 10.0.2.2 is a routable LAN address.
     Emulator cleartext stays debug-only. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
