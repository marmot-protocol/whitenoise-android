package dev.ipf.whitenoise.android.notifications

/**
 * Custom Direct Share category for conversation shortcuts. Must match the
 * `<category>` declared in [R.xml.shortcuts] and every dynamic shortcut that
 * participates in Direct Share.
 */
const val CONVERSATION_SHARE_TARGET_CATEGORY = "dev.ipf.whitenoise.android.category.CONVERSATION_SHARE_TARGET"
