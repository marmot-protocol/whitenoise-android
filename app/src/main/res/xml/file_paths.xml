<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Temp files we create for camera capture and outgoing share intents. -->
    <cache-path name="camera" path="camera/" />
    <cache-path name="shared_media" path="shared_media/" />
    <cache-path name="video_attachments" path="video_attachments/" />
    <!-- Signed APKs downloaded for in-app self-update (Zapstore flavor only). -->
    <cache-path name="app_updates" path="app_updates/" />
</paths>
